{
  "name": "mcp-probe-kit",
  "version": "1.0.0",
  "description": "Cursor Development Enhancement Toolkit - MCP Server with 21 practical tools",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "tsc && node build/index.js",
    "test": "npm run build && node test-server.js",
    "inspector": "npx @modelcontextprotocol/inspector node build/index.js",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "ai-tools"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}

