# 会话 7：排查历史会话反复错误与架构问题

**Session key:** chatgpt-2026-08-04-kyle
**Created:** unix:1785803968
**Updated:** unix:1785825803
**Status:** active

## 继承的历史摘要

> 另有 1 个较早会话未展开，可通过 all_history_summary 读取。

### 会话 2（docs/history-session/2.md）

用户核心目标: - 继续 MCP Probe Kit v4 下一步开发，完成并提交 SDK v2 双协议兼容阶段，然后进入下一阶段。 - 继续下一阶段开发，完成 Memory Project Knowledge 与 Shared Experience 分层。 - 继续下一阶段开发，完成 Memory 负面经验数据模型、生命周期和主链路接入。 - 继续完成 MCP Probe Kit v4 的 Memory 2.0、Converge、Plan Heartbeat 与 resume_plan 开发，完成验证、历史记录和代码提交。；已确认事实: - SDK v2 会按协议时代自动投影 capabilities：Legacy 客户端可见旧 tasks，Modern 客户端不会看到旧 tasks。 - Modern input_required、Legacy SDK shim、无 elicitation 降级、拒绝/取消路径均通过真实客户端测试。 - 生产 build/index.js 双时代冒烟通过：Legacy Task completed；Modern input_required 一轮收敛；Modern Task 请求同步降级。 - 旧 @modelcontextprotocol/sdk 顶层依赖已移除。 - 现有 Memory 只有单一 Qdrant 池，sourceProject 此前仅影响来源展示，不影响排序。 - memory-client.ts 原为 531 行，拆出 memory-ranking.ts 后已降至 499 行。 - 搜索文本和结构化结果现已标注 project/shared 范围及权威规则。 - 旧去重逻辑只按正文 Hash，会把不同类型或不同项目范围的资产错误合并，现已修正。 - 旧 Qdrant payload 生命周期字段可缺省，读取时归一为 active 和空数组，保持向后兼容。 - 写入或更新时会立即计算有效状态；审计检索显示 expired/superseded 的实际状态。 - 两份 Memory 单元测试此前被 .gitignore 明确排除，现已纳入 CI。 - 原有 start_* 与 SRC-8 会在 converge 前直接调用 memorize_asset，与 v4 正式记忆后置规则冲突，已统一改为候选提取。 - 原构建期 sync-ui-data 上游请求没有默认超时，网络异常时可无限等待；已增加超时与缓存降级。 - Canonical Skill、Tool Registry、Schema、README 与运行时计划必须共用同一 Converge 后置记忆语义，否则 Agent 可能绕过闸门。 - Tool Registry 当前总数为 33，workflow 工具集为 32，core 为 12，ui 为 4。 - Memory 2.0 / Converge / 恢复阶段已完成并提交。 - 长期记忆写入规则已统一为：验证后准备 MemoryCandidate，Converge 通过后才调用 memorize_asset。 - 构建期 UI 上游同步已具备有界超时和安全缓存降级，不再无限阻塞。；已完成修改: - README.md - package.json - package-lock.json - scripts/mcp-parent-child-smoke.mjs - scripts/mcp-sdk-handshake.mjs - scripts/mcp-v2-dual-era-smoke.mjs - src/index.ts - src/lib/gitnexus-bridge.ts - src/lib/project-detector.ts - src/protocol/ - src/resources/register-resource-handlers.ts - src/server/create-server.ts - src/server/register-tool-handlers.ts - src/server/tool-registry.ts - src/tasks/legacy-task-adapter.ts - src/tasks/__tests__/legacy-task-protocol.integration.test.ts - src/tasks/__tests__/task-adapters.unit.test.ts - docs/specs/mcp-v4/compatibility-matrix.md - docs/specs/mcp-v4/subspecs/protocol-compatibility/tasks.md - docs/specs/mcp-v4/tasks.md - src/lib/memory-ranking.ts - src/lib/memory-config.ts - src/lib/memory-client.ts - src/lib/memory-orchestration.ts - src/tools/search_memory.ts - src/lib/__tests__/memory-config.unit.test.ts - src/lib/__tests__/memory-ranking.unit.test.ts - src/lib/__tests__/memory-degradation.unit.test.ts - docs/specs/mcp-v4/subspecs/memory-convergence/tasks.md - .gitignore - src/lib/memory-model.ts - src/lib/memory-embedding.ts - src/lib/memory-payload.ts - src/schemas/memory-tools.ts - src/schemas/output/memory-tools.ts - src/tools/memorize_asset.ts - src/tools/update_memory_asset.ts - src/lib/__tests__/memory-model.unit.test.ts - src/lib/__tests__/memory-client.unit.test.ts - src/lib/__tests__/memory-orchestration.unit.test.ts - src/lib/__tests__/memory-payload.unit.test.ts - src/tools/__tests__/memorize_asset.unit.test.ts - src/tools/__tests__/update_memory_asset.unit.test.ts - src/plans/ - src/tools/plan_heartbeat.ts - src/tools/resume_plan.ts - src/tools/converge.ts - src/s…（摘要已截断）

### 会话 3（docs/history-session/3.md）

用户核心目标: - 继续完成 MCP Probe Kit v4 发布前收口，建立 Agent Evals、发布闸门、迁移说明和客户端兼容矩阵，并完成测试与提交。 - 完成 MCP Probe Kit v4.0.0-rc.1 准备；在进入 RC 前执行完整测试，并以真实本地 Agent 和安装后 npm tarball 形态调用 MCP 验收。 - 继续开发 MCP Probe Kit v4.0.0-rc.1，直到达到可发布的稳定 RC 标准。 - 继续开发 MCP Probe Kit v4.0.0-rc.1，直到具备可发布的稳定 RC 资格。；已确认事实: - 原 v4 规格缺少独立 release-readiness 子规格，已新增 FR-9 和第 5 个子规格。 - Reference client 自动验证不能替代真实宿主客户端；Cursor、Claude Code、VS Code Copilot、Cline、OpenCode 和 MCP Inspector 均保持 pending。 - 现有 package 版本仍为 3.7.0；发布静态检查将正式提升到 4.x 记录为 warning，不伪装为已发布。 - 网站迁移入口此前仍指向 v2→v3 旧页面，已新增并切换到 v3→v4 页面。 - 真实本地 Agent 验收首次发现包含“实机验收”的完整发布摘要被误路由到 spec/check_spec；已收紧 spec 触发词并增加回归测试。 - 真实 Agent 按完整上下文调用 workflow 与 start_feature 后，自动选择 parent-child，布局评分为 5，并包含 decompose-spec。 - 未完成计划被 converge 正确拒绝且 memoryWriteAllowed=false；证据齐全的完成计划通过收敛并允许后置记忆写入。 - 实际 npm tarball 安装到临时消费者项目后可从安装目录启动 Modern MCP，发现 33 个工具并正确路由到 start_feature。 - RC 版本固定映射 npm next，稳定版才使用 latest；RC GitHub Release 标记 prerelease，且跳过正式 MCP Registry。 - Node 20.20.2 下全量 81 个测试文件/383 项、双协议、真实 Agent 验收和稳定性循环全部通过。 - 稳定性循环覆盖 16 个场景、81 次 Workflow 调用、冷启动、热调用、4 路并发、Memory 故障降级和协议拒绝，失败数为 0。 - npm 生产依赖审计为 0 vulnerabilities。 - npm 上的 v3.7.0 可重新安装并完成 Legacy 握手，暴露 30 个工具及 start_feature/start_bugfix/check_spec。 - MCP Inspector 2.0.0 CLI 连接生产构建并发现 33 个工具。 - Parent-Child Spec 闸门最终通过：9 条 FR、5 个子规格、0 错误、0 警告。 - 最终 npm run release:verify exit 0。 - 稳定性循环最终 16 个场景、81 次 Workflow 调用、0 失败。 - Node 20.20.2 下 81 个测试文件/383 项、双协议、真实 Agent 和稳定性循环通过。 - MCP Inspector 2.0.0 连接生产构建并发现 33 个工具。 - v3.7.0 npm 回退包 Legacy 握手成功并发现 30 个工具。 - 生产依赖审计 0 vulnerabilities。 - Parent-Child Spec 通过：9 FR、5 子规格、0 错误、0 警告。；已完成修改: - src/evals/ - src/release/ - scripts/run-agent-evals.ts - scripts/verify-release-readiness.ts - package.json - docs/migration-v3-to-v4.md - docs/pages/migration-v4.html - docs/index.html - docs/pages/all-tools.html - docs/pages/examples.html - docs/pages/getting-started.html - docs/specs/mcp-v4/requirements.md - docs/specs/mcp-v4/spec-manifest.json - docs/specs/mcp-v4/tasks.md - docs/specs/mcp-v4/compatibility-matrix.md - docs/specs/mcp-v4/subspecs/release-readiness/ - .github/workflows/release.yml - .github/workflows/publish-mcp-registry.yml - CHANGELOG.md - package-lock.json - server.json - tools-manifest.json - src/release/release-channel.ts - src/release/release-readiness.ts - scripts/verify-release-tag.ts - scripts/agent-local-acceptance.mjs - scripts/package-install-smoke.mjs - scripts/sync-version.mjs - scripts/extract-changelog-section.mjs - src/lib/dev-workflow-routing.ts - src/lib/__tests__/dev-workflow.unit.test.ts - src/release/__tests__/release-channel.unit.test.ts - src/release/__tests__/release-readiness.unit.test.ts - docs/specs/mcp-v4/subspecs/release-readiness/tasks.md - .github/workflows/ci.yml - scripts/stability-soak.mjs - scripts/rollback-smoke.mjs - scripts/inspector-smoke.mjs - docs/rc-stability-policy.md - docs/specs/mcp-v4/subspecs/release-readiness/spec.md；关键设计决定: - Agent Evals 采用确定性输入和硬断言，覆盖 routing、parameter-construction、plan-compliance、memory-…（摘要已截断）

### 会话 4（docs/history-session/4.md）

用户核心目标: - 恢复 mcp-probe-kit v4.0.0-rc.1 的稳定 RC 发布准备，继续完成真实宿主兼容核验并重新跑最终发布闸门。 - 继续开发 MCP Probe Kit v4 RC：收敛默认工具面，保留高价值场景和原子工具，正式接入 MCP Apps，并把 Memory 做成可浏览和搜索的 GUI。 - 逐个模拟真实 Agent 调用全部 MCP 工具，核验 Agent 是否理解返回指导、协议是否正确、文本与 structuredContent 是否信息对称，并修复后重新完成 rc.2 发布闸门。 - 完成 rc.2 发布前的真实 Claude Code 逐工具契约审计，确认每个工具可调用、指导可读、文本与 structuredContent 对称、后续步骤可执行。 - 完成 v4.0.0-rc.2 的真实 Agent/Host 验收并进入发布流程。 - 根据 Cursor 中实际渲染的 Feature Workbench 和 Memory Center 截图，整体重做 MCP Apps 视觉与交互，使开发计划按真实执行进度逐步前进，并修复 Cursor Host 验收发现的 Memory 文本契约问题。 - 让 MCP Apps 在不同 AI Host 和不同对话框宽度下自适应，避免窄容器中双栏、横向 Stepper、按钮和状态区挤压变形。 - 将 MCP Apps 从网页版大留白布局改为适合有限 Agent 对话容器的紧凑、自适应工具面板，并继续提升易用性。 - 继续优化未发布的 4.0.0-rc.3 MCP Apps，使其更像编辑器内嵌工具面板：缩小标题、正文和按钮，优先使用 Host/系统字体，同时保持响应式和主题适配。 - 将 MCP Apps 从展示型网页压缩为高信息密度、实用优先的嵌入式 Agent 工具面板。 - 继续将 MCP Apps 从展示型页面压缩为只保留必要信息的高密度编辑器工具面板，暂不发布。 - Configure npm Trusted Publishing with GitHub Actions OIDC after the user created the trusted publisher connection on npm. - 修复 mcp-probe-kit 升级后项目内 .agents/skills/mcp-probe-kit/SKILL.md 未随安装版本自动更新的问题。 - 根据 rc.3 完整验收报告修复 workflow 对包含规格阶段的 feature 意图误路由，以及 Product Workbench 无法从 description 获取目标用户和核心约束的问题。 - 完成 mcp-probe-kit v4.0.0-rc.4 的修复、真实 Agent 验收、干净安装包验证、CI 和 prerelease 发布。 - 重构 mcp-probe-kit 的 UI 设计工具，使其从风格标签和模板拼装升级为视觉方向、页面结构、真实截图评审与自动迭代闭环。 - 使用 Claude 真实运行 UI 工作流 v2，并通过截图查看实际效果。 - 继续迭代 MCP Apps UI，并使用 Claude 调试优化，最终达到专业、克制、可读且不过度设计的平衡效果。 - 继续迭代 MCP Apps UI，使用 Claude/真实 Host 调试，达到平衡效果后完成 rc.4 候选验证、提交并推送开发分支，但不创建标签或发布。 - 继续迭代并验证 rc.4 MCP Apps 在真实 Host 中的效果，确认是否达到发布前要求。 - 复核 v4.0.0-rc.4 当前 MCP Apps UI 是否仍需优化，并只修复有证据的实际问题。；已确认事实: - 当前分支为 develop/v4；恢复时 HEAD 为 058af88。 - Claude Code 2.1.179 使用严格本地 MCP 配置真实调用 RC 生产构建：workflow 返回 scenario=feature、firstTool=start_feature、spec_layout=auto，并完整保留需求意图。 - Claude Code 真实调用 plan_heartbeat、resume_plan、converge；heartbeatStored=true、resumeFound=true、convergePassed=true、memoryWriteAllowed=true。 - Cursor 3.0.16 CLI 暴露 --add-mcp 和 agent，但该安装没有可自动化的终端 Agent 结果入口，无法完成无头实机调用。 - VS Code 1.104.1 安装了 github.copilot-chat 0.31.5，隔离 MCP 注册成功；Copilot Chat 日志显示已登录后出现 Copilot extension not found，因缺少基础 github.copilot 扩展而未进入 MCP 工具执行。 - Cline 未安装：VS Code 扩展列表无 Cline，PATH 上也无 cline CLI。 - OpenCode 1.17.11 的本地 RC MCP 连接成功；模型驱动调用被外部 models.dev 获取超时阻断，未进入推理或工具执行。 - 最终 HEAD 上完整 npm run release:verify 通过，未发现 RC 服务端回归。 - 当前分支 develop/v4，版本已升级为 4.0.0-rc.2，本轮完成后本地 HEAD 为 a6d1322。 - 默认 compact 模型工具面固定为 23；完整配置 Memory 后动态增加 6 个工具为 29；MCP_TOOLSET=full 保留 33 个兼容模型工具。 - Apps-capable Host 的原始 tools/list 额外包含 list_memory_assets，标记 _meta.ui.visibility=["app"]；compact 为 24 raw/23 model，full 为 34 raw/33 model。 - 正式 MCP Apps 使用 io.modelcontextprotocol/ui 与 text/html;profile=mcp-app；包含 Memory Center、Feature Workbench、Bug Workbench、Product Workbench、Convergence Gate。 - Memory Center 支持历史浏览、分页/类型/状态/项目/标签过滤、语义搜索、详情读取、标记 stale 和确认删除。 - Claude Code 2.1.179 在 MCP_PROTOCOL_MODE=auto 下实际协商 Legacy 2025-11-25；强制 modern 会被该 Host 以 Unsupported protocol version 拒绝，这是 Host 协议能力而非服务器回归。 - Claude Code 2.1.179 对 rc.2 实际调用 workflow 成功，得到 sce…（摘要已截断）

### 会话 5（docs/history-session/5.md）

用户核心目标: - 排查并修复 mcp-probe-kit 4.0.0-rc.3/rc.4 在 Windows MCP 客户端通过 npx 安装后因 @modelcontextprotocol/server/stdio 缺失而无法启动的问题，并发布可验证的新 RC。 - 开发在宿主 Agent 无法看到 MCP 工具时的自动 CLI 降级方案，并确保 Windows、macOS、Linux 跨平台兼容。；已确认事实: - 用户日志中的 rc.4 ETARGET 首先包含版本号误写 4.0.0-rc4，但修正为 4.0.0-rc.4 后仍稳定复现 ERR_MODULE_NOT_FOUND。 - rc.3/rc.4 的 package.json 依赖元数据正确；真正发布缺陷是运行入口依赖 npx 完整安装 SDK 依赖树，用户环境会反复得到不完整 _npx 树。 - 最初将 esbuild bundle 放在 postbuild 不可靠：npm pack 会触发 prepare -> npm run build，并用 tsc 生成的外部依赖入口覆盖 postbuild bundle。 - 最终将 tsc、esbuild 自包含 bundle、UI 数据复制全部纳入 build 本体；发布入口不再包含 @modelcontextprotocol/server/stdio 外部导入。 - 宿主或第三方 Agent Provider 可能连接 MCP 但不向实际 Agent 会话桥接工具，单纯重启 MCP 无法修复该路径。 - 项目内版本锁定 CLI 可以绕过宿主工具租约层，同时保持与原生 MCP 完全相同的 Tool Registry、计划状态和收敛语义。 - 原发布与 CI 工作流只有 Ubuntu；已新增 macOS runner，避免将 Linux POSIX 验证误称为 macOS 验证。；已完成修改: - package.json - package-lock.json - scripts/bundle-runtime.mjs - scripts/package-install-smoke.mjs - CHANGELOG.md - apps/mcp-probe-app.ts - src/lib/mcp-apps.ts - src/lib/__tests__/mcp-apps.unit.test.ts - src/resources/generated/mcp-app-bundle.ts - server.json - tools-manifest.json - docs/i18n/*.json - docs/index.html - docs/pages/*.html - src/cli/command-line.ts - src/lib/cli-fallback-installer.ts - src/index.ts - src/lib/workflow-skill-installer.ts - src/lib/workflow-skill-template.ts - src/lib/harness-adapters.ts - src/lib/harness-skill-targets.ts - src/lib/agents-md-template.ts - .github/workflows/ci.yml - README.md；关键设计决定: - 不覆盖已发布的 rc.4，升级并发布 4.0.0-rc.5。 - 使用 esbuild 生成 Node 20 ESM 单文件运行时，并注入 createRequire 兼容 cross-spawn 等 CommonJS 动态 require。 - 增强 package-install-smoke：安装 tarball 后删除除 mcp-probe-kit 外的全部 node_modules，再执行真实 Modern MCP 握手、列出工具并调用 workflow。 - 同时纳入此前已验证的 MCP Apps 无计划状态修复：不显示误导性的 0/0 已完成，且无 plan_id 时隐藏刷新/收敛操作。 - CLI 直接复用现有 Tool Registry 和工具处理器，不复制业务实现。 - bootstrap 同时生成 Windows probe.cmd/probe.ps1 与 macOS/Linux POSIX probe 启动器，并锁定精确包版本。 - Skill、AGENTS.md 和 Cursor alwaysApply 规则优先原生 MCP，工具不可见时自动降级到项目内 CLI。 - 旧版本进程不得覆盖项目中更高版本的 Skill、AGENTS、规则、manifest 或启动器。 - 增加 macos-latest CI 任务，实际执行打包后的 POSIX CLI 启动器。；测试结果: - npm run release:verify：完整通过。 - Vitest：91 个测试文件、415 个用例全部通过。 - 静态发布闸门：29/29，通过且无警告。 - 工具契约审计：38/38。 - 协议烟测：Legacy、Modern 与 full 33 工具兼容面通过。 - 稳定性循环：16 个场景、81 次 workflow 调用、0 失败。 - Agent evals：26/26。 - Inspector 2.0.0 烟测通过。 - 生产依赖 npm audit：0 vulnerabilities。 - 安装包无依赖烟测：删除全部运行时依赖后仍完成握手、列出 23 工具并调用 workflow。 - npm 注册表全新缓存 npx mcp-probe-kit@4.0.0-rc.5 实际启动成功。 - npm run release:verify 全部通过：29 项静态检查、93 个测试文件/424 项测试、38/38 工具审计、协议烟测、Agent 验收、16 场景/81 调用稳定性循环、26/26 Agent eval、打包、回滚、Inspector、安全审计。 - 独立 npm tarball 删除外部 MCP SDK 依赖后，原生 MCP 与 CLI 均可运行。 - 三个独立 CLI 进程完成 plan_heartbeat → resume_plan → converge。 - npm registry 实装 rc.6：install-agent、exec workflow、runtime manifest、Windows/POSIX 启动器精确版本锁定均通过。 - GitHub Actions macos-cli-fallback 在 macos-latest 上 Build 与 Verify packaged POSIX CLI fallback 均通过。；当前运行状态: - 分支 develop/v4；提交 1d7215d（fix: bundle runtime dependencies for rc5）已推送。 - 标签 v4.0.0-rc.5 已推送。 - GitHub Release workflow 307405…（摘要已截断）

### 会话 6（docs/history-session/6.md）

用户核心目标: - 发布 mcp-probe-kit 的新 RC 版本；rc.7 标签触发后因 Node 20 发布闸门失败，遵循不可移动标签策略修复并发布 rc.8。 - 完善 rc.8 的 README、官网文档、多语言和 MCP Apps 动态展示，并使用 Playwright 审计和修复所有文档页面的一致性。 - 将所有工具页的分类目录恢复为旧版左侧菜单展开方式，删除主内容区重复分类栏。 - 修复动态 GIF 自身底部大面积留白，以及官网首页和 Apps 详情页对演示图片/工作台的异常缩放、固定高度和不可读展示。 - 使用本地 build/index.js 让 Cursor 验收 App-only 工具隔离修复，避免继续发布大量 RC；同时提交 README 致谢。 - 根据 Cursor 本地功能验收报告修复 workflow 路由、无效项目路径落盘、Memory 错误可观测性和非 Git 工具行为；继续使用本地 build 测试，不发布新 RC，不触发 GitHub Workflow。 - 根据 Cursor 完整功能验收与 Memory Center 实际 Host UI 错误，逐项修复阻断和 P2/P3，要求每项独立测试、独立提交，避免修一项新增另一项缺陷；本阶段不 push、不发布。；已确认事实: - v4.0.0-rc.7 Release run 30784310072 失败，原因是三项测试/显式命令路径错误假设当前 Node 支持托管 GitNexus；rc.7 未发布到 npm。 - 修复包括：显式 MCP_GITNEXUS_ARGS 存在时不调用 defaultGitNexusNpxArgs；托管安装与超时测试显式模拟 Node 22；doctor 测试按当前 Node 版本断言 managed_pending 或 managed_unsupported。 - rc.8 Release run 30785017814 成功；CI run 30784973741 成功。 - Windows、macOS、Linux GitNexus Sidecar 三平台发布矩阵全部成功。 - GitHub prerelease v4.0.0-rc.8 已发布，release id 363991771，prerelease=true。 - npm mcp-probe-kit@4.0.0-rc.8 已发布；dist-tags 为 latest=3.7.0、next=4.0.0-rc.8。 - 初始 Playwright 审计发现三套页面壳并存：landing/apps、旧 docs shell、独立 migration-v4；examples 和 migration 仍使用 max-w-4xl。 - 共执行 36 张初始截图、21 张修复后截图、18 张最终截图，覆盖 1600x1000、1024x900、390x844。 - 最终 Playwright 几何结果：桌面六页 H1 均为 (296,132)，平板均为 (288,124)，手机均为 (16,108)。 - 最终六页三尺寸均为 0 横向溢出、0 浏览器控制台错误、每页 1 个语言切换器；顶部栏统一 56px，桌面/平板侧栏统一 256px。 - README 及七个翻译 README 已加入 Feature、Memory、Convergence GIF，并链接五个实时 MCP App 演示。 - 工具文档由真实 Tool Registry 自动生成 33 个 full 工具和 1 个 App-only 动作，四语均有一句话调用。 - 通过 git 历史确认旧版结构为 toggleToolsMenu + tools-submenu，并按该模式恢复。 - Playwright 桌面实测：10 个分类、34 个工具链接，重复内容分类面板为 0，侧栏 256px，主内容 x=256，工具内容宽 1264px。 - Playwright 收起/展开测试：aria-expanded 在 true/false 间正确切换，submenu hidden 状态正确。 - Playwright 手机实测：抽屉关闭时 x=-256，打开时 x=0；点击 init_project 后抽屉关闭并跳转 #init-project。 - 桌面和手机均无横向溢出、无控制台或 pageerror。 - 原始桌面 GIF 画布为 Feature 1180x720、Memory 1180x720、Convergence 1180x560，但真实内容最大高度仅约 412、306、303px。 - 原 Apps 详情 iframe 高度为 770/770/770/599/599px，真实内容分别仅 319/349/310/262/307px。 - 初次仅改自然比例后，Memory/Convergence 在桌面两列中只有约 562x165，手机仅约 356x105，仍不可读，因此进一步改为全宽与手机专用 GIF。 - 最终桌面 GIF：Feature 1108x430、Memory 1108x324、Convergence 1108x321；手机 GIF：390x550、390x528、390x352。 - 六张 GIF 所有帧最终底部留白均固定为 18px，没有内容裁切。 - 最终首页桌面完整演示显示尺寸为 1150x446、1150x336、1150x333；手机实际加载专用素材并显示为 356x502、356x482、356x321。 - 最终 Apps iframe 与真实内容高度仅差 2至3px；桌面和手机均无横向溢出、无浏览器控制台错误。 - Cursor 3.0.16 本地连接 mcp-probe-kit-local 成功。 - Memory 启用、compact、MCP Apps 协商后 tools/list 精确为 29，list_memory_assets 不暴露。 - 6 个 Memory 模型工具完整，5 个 MCP Apps 资源正常注册。 - workflow、heartbeat、resume、converge 正反向均通过，消费项目无污染。 - MCP Apps Host UI 内 App-only 实调仍 UNVERIFIED，因此整体为有条件通过。 - 启动 stderr 的 workspace=e:\workspace\github\coding-tools-mcp-rust，而 probe://status.projectRoot=e:/workspace/test/tests6，存在根目录来源差异，后续需单独核查。 - Cursor 报告的 workflow 两个错误路由均已在本地 build 真实复测修复。 - Cursor 原配置指向不存在的 Ollama 11434；本机实际已有 Docker Qdrant 50008 和 Infinity/Nomic 50012，embedding endpoint 为 /embeddings 且需要 INFINITY_API_KEY。 - 使用现有 Infinity 与 Qdran…（摘要已截断）

## 用户核心目标

- 读取既有开发历史并诊断 MCP Probe Kit 为何持续出现新错误，判断是否存在架构层问题。
- 以最小范围修复工具契约审计与 App-only 可见性行为不一致，并使用本地新编译版本通过自动化、协议和 Claude 全功能验收，确保现有功能不退化。
- 全面反思 MCP Probe Kit 当前能力缺口，避免继续按用户逐条提醒才局部补功能，并重新定义如何发挥 MCP 的最大价值。
- 完整设计 MCP Probe Kit Engineering Control Plane V1，明确任务等级由谁判断、Agent 如何执行，以及如何以实际工程问题为中心整合图谱、计划、证据、闸门和跨 Host 一致性。
- 纠正 Engineering Control Plane 设计中过度依赖 workflow 作为主入口的问题，明确真正可靠的统一入口与跨 Host 执行路径。
- 重新校准 TaskRiskClassifier 的职责边界：用户认为 MCP 无法可靠判断需求层级，应该由 Agent 负责判断，MCP 主要提供指导架构。
- 纠正简化方案中弱化 Memory 的问题，明确长期记忆在统一工程纪律中的必要角色。
- 形成一份不再丢失 Memory、状态、验证或工具组合的完整最终需求，明确现有 33 个工具各自职责、主流程、组合方式和实现顺序。
- 纠正最终需求中遗漏的 start_bugfix SRC-8 八步法和架构工具，确保两者进入正式产品基线并与 Memory、Plan、验证和 Converge 组合。
- Clarify the architecture: start_* tools are workflow orchestrators, not intent recognizers; fix_bug owns SRC-8, while start_bugfix composes the full bugfix delivery flow.
- Clarify that not every atomic capability belongs in a start_* orchestration; atomic, composable, and actually orchestrated are different concepts.
- Produce the final logical architecture diagram for MCP Probe Kit after clarifying direct atomic usage versus orchestrated start_* flows.
- Produce the complete pre-development requirements acceptance baseline: every tool, its current implementation, intended effect, change status, final counts, and end-to-end acceptance scenarios.
- Correct the final acceptance baseline by adding the missing architecture-specific methodology, not just architecture modes.
- Merge all settled design decisions into one complete, detailed, authoritative requirements and acceptance document before development starts.
- Begin engineering-grade implementation from the accepted requirements, starting with Phase 0 compatibility baseline and no production behavior changes.
- Complete Phase 1 responsibility alignment with engineering-grade maintainability while preserving all public tool surfaces and behavior contracts.
- Begin Phase 2 with an engineering-grade shared ARC-8 methodology core and schemas before exposing the public architecture tool.
- Continue development and complete Phase 2 by productionizing the architecture tool and ARC-8 with maintainable engineering structure, full regression, protocol verification, and an independent commit.
- Continue MCP Probe Kit delivery-system implementation, complete Phase 3 Plan lifecycle changes, and investigate/fix the MCP App workbench bug where completed Feature/Bug work still displayed every Plan step as pending after refresh.
- Continue Phase 4 with an engineering-grade, independently reversible closure of the start_ui delivery orchestrator before moving to start_onboard.
- Continue Phase 4 by closing start_onboard as a complete, maintainable and independently reversible project onboarding workflow.
- Continue Phase 4 by closing start_product as a formal product artifact and prototype delivery workflow without falsely requiring software implementation evidence.

## 已确认事实

- 问题属于控制面/契约架构缺陷，不是核心运行时仍为单体。src/index.ts 已约 4KB，Tool Registry 也已模块化。
- 当前存在可复现的契约冲突：生产代码与 MCP Apps 集成测试要求 list_memory_assets 不进入 tools/list，但 scripts/tool-contract-audit.mjs 仍断言它必须出现在 tools/list。
- npm run audit:tools 当前失败，错误为 Apps tools/list missing list_memory_assets；而 4 个相关定向测试 37 项全部通过，npm run release:static 及 docs:verify 1067 项也通过，证明各闸门维护了不同的事实模型。
- README、compatibility-matrix、inspector-smoke、历史文档中仍保留旧的 raw tools/list 24/30 与 list_memory_assets 可出现的约定，和当前运行时语义不一致。
- src/lib/dev-workflow-routing.ts 仍以重叠正则、数组顺序和分数平局决定路由；例如同时含“错误”和“架构”的意图会同时命中 bugfix/explore，并因规则顺序偏向 bugfix。
- Tool Catalog、输入 Schema、独立 Output Schema 注册表、App-only Schema、可见性、发布审计和文档仍分散维护，单一事实源只完成了一部分。
- 任意普通工具调用前仍会执行带写盘副作用的 bootstrap；bootstrap 异常被记录后返回 null，工具继续执行，可能造成部分初始化和隐性状态差异。
- 本地 develop/v4 的 HEAD 为 e8bc11a，origin/develop/v4 仍在 03bd681；本地约有 10 个修复提交尚未进入远端，叠加本地 build、npm 包、CLI fallback 多运行入口，存在版本身份漂移风险。
- 生产代码与 MCP Apps 集成测试均要求 list_memory_assets 不进入 tools/list；失败来自 scripts/tool-contract-audit.mjs 的旧断言。
- 本批未修改生产运行时、Tool Registry、MCP Apps 注册、Memory 实现、路由或 Bootstrap。
- 新编译 build/index.js 的 deterministic audit 38/38 通过，工具面保持 compact=23、compact+Memory=29、full=33、Apps raw=29。
- Claude Code 2.1.179 按 7 个有界批次真实调用本地 build/index.js，33/33 模型可见工具全部通过，0 assessment failure，missing/unexpected 均为空。
- 单次聚合 Claude 审计超过当前执行器 10 分钟上限且无中间进度；拆分批次后全部完成，属于测试执行可观测性/时长问题，不是功能失败。
- 此前分析方式过于问题驱动和局部修复，缺少从完整软件交付生命周期、Agent 行为约束和产品护城河角度进行能力地图与缺口分析。
- 当前项目已有 workflow、start_*、code_insight/GitNexus、Spec、plan_heartbeat、converge、Memory、Claude 验收等组件，但它们仍主要是松散工具和建议链，缺少统一的权威项目模型、任务风险分级、强制生命周期状态机、架构与数据所有权闸门以及修改前后漂移验证。
- start_feature 已接入任务级图谱和 graph-insights 基线，但图谱目前更多用于提供上下文线索，没有成为架构规则、数据所有权、公共契约、测试覆盖和变更影响的统一证据底座。
- MCP Probe Kit 最大价值不应是替 Agent 生成更多代码，而应成为跨宿主、跨 Agent 的软件工程控制面：让任何 Agent 都按可恢复、可验证、可审计、可维护的方式交付软件。
- 核心缺口应统一定义为 Engineering Control Plane 与 Project Digital Twin，而不是继续零散增加 start_architecture、check_architecture 等孤立工具。
- 现有 workflow 主要做任务类型路由，缺少任务风险等级、分阶段重判和 implementationAllowed 状态。
- 现有 start_feature 已有任务级图谱和 graph-insights 基线，但图谱目前主要作为提示线索，尚未成为任务分类、数据所有权、公共契约、测试覆盖和漂移检查的统一事实模型。
- 现有 Plan Evidence 固定为 requirements/spec/implementation/test/review 等少量类型，converge 使用固定证据组，无法按 L0-L3 动态治理。
- MCP 无法在所有 IDE 中物理阻止 Agent 直接写文件，因此设计必须区分可强制 Host hook 与不可强制 Host；后者至少做到检测越界、阻断收敛和接管/回滚恢复。
- 任务类型与风险等级必须分离，同一 feature/bugfix/ui 可分别是 L0-L3。
- 当前 workflow 的实现本质上是正则和优先级驱动的场景路由器，适合给出 firstTool，不适合作为风险分类和工程治理的唯一信任边界。
- Agent 可能直接调用 start_feature、start_bugfix、refactor 或 CLI fallback；若分类逻辑只挂在 workflow 下，所有直达路径都会绕过统一治理。
- 公开工具入口与内部治理入口必须分离：公开工具可有多个，内部必须只有一个 ToolInvocationGateway/PolicyKernel。
- MCP 不能看到完全绕过 MCP 的外部文件编辑，但可以通过基线 revision、真实 diff、heartbeat 和 converge 检测未规划修改并阻断收敛。
- 用户指出的核心问题成立：MCP 不能仅凭需求文本可靠判断业务重要性、真实复杂度和风险容忍度，尤其在未理解项目和业务上下文时。
- 将 TaskRiskClassifier 设为最终权威判定者存在过度自动化风险，也与 MCP Probe Kit 作为指导与工程治理架构的定位冲突。
- 需求语义判断、业务重要性和方案复杂度更适合由 Agent 结合对话、代码和图谱进行推理；MCP 更适合提供结构化评估框架、强制证据字段、客观工程事实校验和流程一致性。
- L0-L3 不应成为驱动流程的单一真相；更可靠的是直接输出 required controls，例如是否需要影响分析、架构设计、Spec、迁移、回滚、全量验证和用户确认。
- MCP 可确定性识别公共契约、数据迁移、权限安全、运行入口、跨模块和回滚等客观风险，但不能独立判断战略重要性、SLA、停机容忍度和隐藏业务规则。
- 将 Memory 仅称为辅助能力会削弱 MCP Probe Kit 的长期价值；没有跨任务记忆，不同 Agent 仍会重复踩坑、重复探索和重复破坏既有约束。
- Memory 的正确位置不是单独堆叠的系统，也不是无条件自动写入，而是贯穿任务前召回、任务中候选、任务后验证沉淀和后续反馈修正。
- 现有 search_memory、memorize_asset、delete_memory_asset 以及 converge 后写入的纪律应保留并成为最小闭环的一部分。
- 用户对前序设计反复摆动和遗漏关键能力不满意，要求锁定一份完整、实际、可执行的最终需求。
- 现有项目已经具备 33 个工具、start_* 自动记忆注入、Plan heartbeat/resume/converge、图谱和验证能力，主要缺口是职责与组合没有形成一份稳定的最终产品基线。
- workflow 适合作为不确定时的路由器，不应成为强制主入口；Agent 负责工程判断，MCP 负责统一方法、项目证据、过程状态、验证和长期记忆。
- Memory 必须贯穿 Recall→Candidate→Validate→Converge→Persist→Reuse→Update/Delete 全生命周期，不能被降级为任务末尾可选项。
- 现有代码已经完整实现 SRC-8：明确差距、收敛边界、验收契约、把握真因、制定对策、贯彻修复、评价双轨、巩固传播；此前最终需求摘要把它压缩成普通 Bug 流程，属于严重遗漏。
- 现有 start_bugfix 的 Delegated Plan 本身承载 SRC-8；fix_bug 是辅助指南，并非 compact 工具面中的必经公开调用。
- 仅使用 code_insight 和 refactor 不能完整覆盖架构评估、目标设计、实施前校验和实施后漂移检查，需要恢复一个统一且克制的 architecture 工具。
- 架构能力不应拆成多个工具；单个 architecture 工具使用 assess、design、validate、drift 四种 mode，并复用 code_insight、Memory、Plan、Converge。
- Current fix_bug.ts already owns SRC-8 artifacts and gates.
- Current start_bugfix.ts composes SRC-8 through mergeBugfixOrchestrationPlan, so the code is partly aligned, but docs and public responsibility wording were blurred.
- The previous wording incorrectly implied that all remaining tools were expected to be invoked through start_* plans.
- The design should be modeled by tool role plus invocation mode, not a strict mandatory hierarchy.
- The final architecture should show two parallel paths: direct atomic invocation and optional orchestrated delivery.
- Shared infrastructure should include Skill, project context/graph, Plan Store, Memory Store, Tool Catalog/Registry, MCP/CLI/Host adapters.
- Current tool audit passed 38/38 with surfaces compact=23, compactWithMemory=29, full=33, appsRaw=29.
- Current unique callable names are 34 because list_memory_assets is app-only and hidden from tools/list.
- start_feature currently closes planning/spec/estimate better than implementation/test/review/converge.
- start_bugfix already composes shared SRC-8, graph, memory, and spec gate but needs explicit review/convergence closure and responsibility wording alignment.
- start_ui has rich visual execution steps but some modes use plain delegated-shaped plans rather than the shared DelegatedPlanContract and lack full test/review/converge closure.
- start_onboard currently orchestrates only init_project_context and should be expanded to code insight, commands, memory, and project navigation.
- Plan state lacks explicit MemoryCandidate/ArchitectureCandidate fields; converge currently uses fixed default evidence kinds rather than plan-driven workflow-specific gates.
- memorize_asset does not hard-enforce converge, which is acceptable for direct/manual memory management; managed workflows must enforce sequencing through their plan.
- The prior acceptance baseline listed architecture modes and outputs but omitted the actual method that tells the Agent how to perform architecture work.
- The existing design document had assess/design/validate/drift and architecture outputs, so ARC-8 can be added without creating more public tools or a central controller.
- Current code audit baseline remains 38/38 with compact=23, compactWithMemory=29, full=33, appsRaw=29.
- The previous document was already structurally strong but lacked the full implementation matrix, exact visibility counts, architecture IO/tests, direct-vs-managed memory distinction, truthfulness/write boundaries, detailed implementation gaps, and explicit sign-off checklist.
- No production source code was modified in this turn.
- Current runtime surfaces exactly match the accepted pre-architecture baseline: Compact 23, Compact+Memory 29, Full 33, Apps model-visible 29, App-only 1, unique callable 34.
- The existing contract audit previously checked representative calls and counts but did not fail on an exact missing/unexpected tool-name mismatch.
- Legacy and modern Compact surfaces currently match exactly.
- No production handler, schema, registry, visibility or runtime behavior was modified in Phase 0.
- The deterministic Agent eval initially detected that revised Catalog wording had dropped critical feature argument, first-heartbeat and convergence evidence terms; those terms were restored without reverting the new responsibility model.
- The generated Skill remains synchronized with all 33 current model-visible tools.
- Exact runtime surfaces remain Compact 23, Compact+Memory 29, Full 33, Apps model-visible 29, App-only 1 and unique callable 34.
- All full-suite and protocol tests pass after the final wording correction.
- The repository has no npm typecheck script, so direct npx tsc --noEmit is the valid compiler check.
- Initial TypeScript compilation caught that alternative inputs were too strict; the input type was corrected to allow concise candidates while preserving complete output normalization.
- Focused ARC-8 tests cover seven method and gate scenarios and all pass.
- The initial architecture-method implementation reached 581 lines, violating the repository maintainability threshold; it was split before commit.
- After the split, architecture production modules are 178, 219, 150, 142, and 360 lines.
- The architecture handler optionally collects code_insight and Memory evidence, preserves fact/inference/unknown distinctions, applies ARC-8 gates, and returns ArchitectureCandidate, ADR Candidate, and MemoryCandidate without claiming implementation completion.
- Release readiness, docs generation, Skill synchronization, Legacy/Modern protocol, CLI registry reuse, and all tool-surface counts recognize architecture consistently.
- The screenshots exposed a real state-tracking defect: the workbench treated a successful resume_plan RPC as a successful state refresh even when no Plan checkpoint existed.
- No .mcp-probe-kit/plans checkpoint files existed for the reported Feature and Bug Plan IDs, so resume_plan had no stored completion state to return.
- resume_plan is intentionally read-only and cannot truthfully infer completed steps from chat text, Git diff or files; plan_heartbeat is the authoritative state writer.
- start_feature steady Plans previously stopped at specification, check and estimation despite claiming full delivery completion, so the workbench did not even have implementation, test and review steps to track.
- The workbench used the raw input project_root instead of the resolved bootstrap project root, which could read or create checkpoints in the wrong workspace.
- Phase 3 now makes convergence Plan-driven and preserves structured artifacts, candidates, acceptance results and runtime evidence compatibly.
- The old start_ui implementation had three duplicated ad hoc plans and separate auto/manual report summaries, so Plan steps, displayed report and execution guidance could drift.
- The old UI flow ended after visual acceptance and lacked explicit state/accessibility validation, actual test evidence, code review, architecture drift and formal convergence guidance.
- Array order was used as implicit execution order in several UI steps; resume_plan requires explicit dependsOn relationships.
- After refactoring, start_ui.ts is 473 lines; start-ui-plan.ts 370; start-ui-request.ts 230; start-ui-config.ts 129; start-ui-output.ts 127.
- The previous start_onboard only wrapped init_project_context in a one-step ad hoc plan and could not produce a genuine project mental model or verified quickstart.
- Onboarding does not require code_review or architecture by default because it is a read-only understanding workflow; code_insight plus evidence-backed Agent analysis is the appropriate capability set.
- The new modules remain small: start_onboard.ts 159 lines and start-onboard-plan.ts 257 lines.
- The previous product flow had no explicit Heartbeat/Resume/Converge protocol and no product acceptance quality gates.
- The previous flow described ui_design_system as writing files even though the tool only returns structured content and guidance.
- The previous parent flow could treat a start_ui tool response as an implemented prototype even though start_ui returns a child Plan.
- The new modules are bounded: start_product.ts 268 lines and start-product-plan.ts 351 lines.

## 已完成修改

- scripts/tool-contract-audit.mjs
- docs/history-session/7.md
- .gitignore
- docs/engineering-control-plane-v1.md
- docs/mcp-probe-kit-delivery-system-v1.md
- config/tool-surface-baseline.json
- scripts/lib/tool-surface-baseline.mjs
- scripts/lib/__tests__/tool-surface-baseline.test.mjs
- package.json
- docs/verification/tool-surface-baseline-v4.0.0-rc.8.md
- docs/implementation/delivery-system-v1-progress.md
- src/server/tool-catalog.ts
- src/lib/mcp-tool-skill-registry.ts
- src/lib/workflow-skill-template.ts
- src/tools/workflow.ts
- src/lib/__tests__/mcp-tool-skill-registry.unit.test.ts
- src/lib/architecture-method.ts
- src/schemas/architecture-tools.ts
- src/schemas/output/architecture-tools.ts
- src/lib/__tests__/architecture-method.unit.test.ts
- src/lib/architecture-types.ts
- src/lib/architecture-normalization.ts
- src/lib/architecture-validation.ts
- src/tools/architecture.ts
- src/tools/__tests__/architecture.unit.test.ts
- src/schemas/index.ts
- src/tools/index.ts
- src/server/tool-registry.ts
- src/server/tool-visibility.ts
- src/lib/output-schema-registry.ts
- src/lib/dev-workflow-routing.ts
- src/lib/dev-workflow.ts
- tools-manifest.json
- docs/data/tools.js
- docs/i18n/all-tools/zh-CN.json
- docs/i18n/all-tools/en.json
- docs/i18n/all-tools/ja.json
- docs/i18n/all-tools/ko.json
- src/lib/delegated-plan-contract.ts
- src/plans/plan-state-metadata.ts
- src/plans/plan-types.ts
- src/plans/plan-heartbeat.ts
- src/plans/plan-store.ts
- src/plans/plan-converge.ts
- src/tools/plan_heartbeat.ts
- src/tools/resume_plan.ts
- src/tools/converge.ts
- src/schemas/plan-tools.ts
- src/schemas/output/plan-tools.ts
- apps/mcp-probe-app.ts
- src/lib/plan-workbench-state.ts
- src/lib/delegated-plan-renderer.ts
- src/lib/mcp-apps.ts
- src/resources/generated/mcp-app-bundle.ts
- src/tools/start_feature.ts
- src/tools/start_bugfix.ts
- src/lib/__tests__/mcp-app-plan-state.unit.test.ts
- src/lib/__tests__/mcp-apps.unit.test.ts
- src/tools/__tests__/start_feature.unit.test.ts
- src/tools/__tests__/start_bugfix.unit.test.ts
- src/tools/start_ui.ts
- src/tools/start-ui-plan.ts
- src/tools/start-ui-request.ts
- src/tools/start-ui-config.ts
- src/tools/start-ui-output.ts
- src/tools/__tests__/start_ui.unit.test.ts
- src/tools/__tests__/start_ui.property.test.ts
- src/tools/start_onboard.ts
- src/tools/start-onboard-plan.ts
- src/tools/__tests__/start_onboard.unit.test.ts
- src/tools/start_product.ts
- src/tools/start-product-plan.ts
- src/tools/__tests__/start_product.unit.test.ts

## 关键设计决定

- 本轮只做架构诊断和复现，不修改生产代码。
- 下一阶段应暂停继续叠加局部正则和兼容分支，先收敛控制面契约。
- 需要建立统一 ToolContract/CapabilityManifest，并由其生成 tools/list、App-only 行为、审计期望、文档计数和兼容矩阵。
- 需要把项目根目录、宿主能力、协议时代、Memory 配置和运行构建身份解析为一次性不可变 ExecutionContext，验证通过后再执行 bootstrap 或外部 I/O。
- 仅修复 scripts/tool-contract-audit.mjs 的过期契约，不进行架构重构。
- list_memory_assets 继续保持 tools/list 隐藏，但在已协商 MCP Apps 后允许按已知名称调用。
- 任何后续架构治理必须独立立项，先锁定行为基线和兼容矩阵，再做小步可回滚改动。
- 本地验证完成后只做本地提交，不 push、tag 或 publish。
- 先做完整能力地图和目标产品架构，再决定是否增加公开工具；避免通过增加大量工具名称掩盖编排与治理缺失。
- 以 Project Digital Twin 作为统一事实模型，整合代码图谱、模块边界、数据所有权、公共契约、运行时路径、Spec、测试、Git/history 和 Memory。
- 以生命周期状态机和策略引擎决定不同任务等级必须经过的上下文、架构、规格、实现、验证、漂移检查和收敛闸门。
- 公开工具面保持克制，优先增强 workflow、start_*、code_insight 和 converge 的内部协作；内部服务按 ProjectModel、ImpactAnalyzer、ArchitecturePolicy、GateEngine、EvidenceStore 解耦。
- 实施采用只读基线与报告优先、L2/L3 渐进启用闸门、验证等价后再收口旧逻辑的方式，禁止一次性大重构。
- 最终 L0-L3 由服务端 TaskRiskClassifier 判定，不由用户菜单或 Agent 主观决定；模型只提取候选信号。
- 分类分三次：workflow 快速预判、start_* 基于影响图确认、执行期基于真实 diff 重判。
- 建立 Project Digital Twin，但 V1 使用版本化快照和任务影响子图，不建设重型中央数据库。
- 公开工具面第一阶段保持克制，先增强 workflow、code_insight、start_*、plan_heartbeat、resume_plan、converge 的内部协作，不立即增加 architecture 工具。
- L0/L1 保持轻量；L2/L3 才强制架构影响、数据所有权、公共契约、漂移和更严格的 Claude 本地 build 验收。
- 实施采用观察模式→统一快照→Plan/Evidence 升级→警告闸门→强制闸门→Host hook/可视化的渐进路线。
- workflow 保留为可选路由助手，不再定义为唯一主入口或分类权威。
- 新增内部设计概念 ToolInvocationGateway/PolicyKernel，所有相关 MCP 工具和 CLI fallback 在进入 handler 前统一经过任务恢复、快速分类、Gate 检查和运行身份注入。
- 直接调用 start_* 与先调用 workflow 再 start_* 必须产生相同的 Task Profile、风险等级、Gate 和状态语义。
- TaskRiskClassifier 作为 PolicyKernel 的内部服务，不属于 workflow。
- 采用 Agent-led、MCP-constrained 模式：Agent 提出 Task Assessment 和建议治理深度，MCP 校验证据、一致性和最低强制控制。
- 将 TaskRiskClassifier 的概念降级为 TaskAssessmentEngine/PolicyAdvisor，不作为最终需求层级裁判。
- 任务等级改为可选派生标签，核心决策改为 requiredControls 与 missingEvidence；converge 检查证据是否闭环，而不是检查某个等级标签是否正确。
- 业务关键性、风险容忍度和无法从仓库推断的约束由用户确认；命中客观高风险事实时 MCP 可以自动增加最低控制要求。
- 高风险任务可要求第二 Agent 或 Claude 独立复核 Assessment，但不让第二模型直接替代事实证据。
- 保留简化后的三层主架构：指导、状态、验证；Memory 作为贯穿三层的第四条纵向能力，而非可有可无的附属功能。
- 任务开始前按任务意图、模块、错误现象和项目范围自动检索相关记忆；任务执行中只记录候选，不立即写入长期记忆；只有测试、审查和 converge 通过后才正式沉淀。
- 记忆至少覆盖已验证架构决策、稳定实现模式、历史 Bug 根因与回归保护、Host/协议兼容坑、数据口径与所有权规则、失败方案与禁用做法。
- 必须支持去重、冲突、过期和删除；新证据可修正或废弃旧记忆，不能把记忆库变成不断累积的垃圾堆。
- 以 docs/mcp-probe-kit-delivery-system-v1.md 作为后续实施的最终需求基线。
- 旧 docs/engineering-control-plane-v1.md 标记为已被取代，自动分级、Policy Kernel 和 Permit 不再作为 V1 实施依据。
- V1 不新增、不删除、不改名现有 33 个公开工具；Agent 只需记住 start_feature、start_bugfix、start_ui、refactor、start_onboard、workflow 六个主要工程入口。
- 现有 33 个工具划分为 6 个主要入口、17 个核心执行与记忆工具、10 个可选辅助工具。
- 统一生命周期确定为 Intake→Recall→Understand→Assess→Specify/Plan→Execute→Verify→Review→Converge→Learn→Commit。
- Agent 完成 Task Assessment；MCP 只检查分析是否完整、是否与真实项目事实和 diff 一致，不自动判断需求层级。
- 入口工具自动召回高相关记忆；执行中通过 plan_heartbeat 保存 MemoryCandidate；只有 converge passed=true 后才允许 memorize_asset。
- 实施顺序固定为兼容基线→职责和主流程→Task Assessment/MemoryCandidate→Heartbeat/Resume/Converge→Diff验证→Memory质量治理。
- start_bugfix 必须严格保留 SRC-8，SRC-4 真因工作表闭合前禁止修改代码，连续三次修复失败返回 SRC-2 或 SRC-4。
- SRC-8 必须生成回归保护和 MemoryCandidate，只有 converge passed=true 后才能正式 memorize_asset。
- 目标工具面从兼容基线 33 个增加到 34 个，只新增一个 architecture 工具，不改名或删除现有工具。
- architecture 成为第七个主要工程入口，并接入 start_feature、start_bugfix、refactor 和独立架构任务。
- 使用过 architecture 的任务，在 converge 前必须执行 validate 或 drift；小型局部修改不强制调用架构工具。
- workflow remains an optional routing helper only.
- All start_* tools are scenario workflow composers that combine atomic/domain capabilities into a Delegated Plan.
- fix_bug is the single source of truth for SRC-8 checklists, gates, worksheets, schemas, and child plan.
- start_bugfix may flatten the fix_bug child steps into its top-level plan for execution and heartbeat, but must not redefine SRC-8.
- architecture remains an atomic domain capability that can be composed by start_feature, start_bugfix, or refactor.
- Atomic tools are independently callable by Agents.
- Only some atomic/domain capabilities are composable by start_* workflows.
- A capability is 'orchestrated' only when a particular Delegated Plan actually selects it.
- start_* is used for complete delivery workflows, not as a mandatory gateway for all tool calls.
- Direct calls such as code_insight, fix_bug, check_spec, architecture, gentest, code_review, memory tools, and git_work_report remain first-class normal paths.
- Agent is the decision maker and chooses tools using Skill plus current context.
- Atomic/domain tools are independently callable; orchestration is optional and task-specific.
- start_* tools are complete-delivery workflow composers, not intent recognizers and not mandatory parents of atomic tools.
- workflow is only an optional first-tool suggestion when Agent is uncertain.
- fix_bug owns SRC-8; start_bugfix composes it into a larger delivery flow.
- Plan, Converge, and Memory are shared capabilities used where needed, not universal mandatory wrappers.
- architecture is an independently callable domain capability and may also be composed into feature, bugfix, or refactor flows.
- Count model-visible and app-only surfaces separately: current full model surface 33 plus hidden app-only list_memory_assets; target full model surface 34 after adding architecture, while unique callable names become 35 including the app-only action.
- workflow is optional routing only; start_* tools are scenario orchestrators; direct/domain tools can be called independently and are composed only when a concrete workflow needs them.
- fix_bug is the public owner of SRC-8 semantics, backed by shared src8 core; start_bugfix composes/expands those steps but does not redefine the methodology.
- Plan and Converge are required for managed long-running or formal delivery flows, not for every single read-only atomic call.
- Memory tools remain directly usable; within managed workflows, MemoryCandidate is persisted only after convergence, while explicit manual memory management remains possible.
- architecture is one new direct model-visible domain tool with assess/design/validate/drift modes and no initial MCP App UI.
- Define one architecture methodology owned by the architecture tool: ARC-8 (Architecture Reasoning & Change 8-step).
- Keep assess/design/validate/drift as entry modes mapped onto ARC-8 stages, not as the methodology itself.
- ARC-8 stages are: define architecture problem; reconstruct current facts; identify structural root cause and protected invariants; generate alternatives; evaluate tradeoffs and decide; design target architecture and transition; pre/phase validation; drift verification and learning.
- architecture is the single public source of truth for ARC-8; start_feature, start_bugfix, and refactor may invoke or reference it but may not copy its steps or gates.
- Architecture gates prevent design without current facts, selection without invariants/root cause, approval without tradeoff evidence, data/contract changes without migration and rollback, and formal convergence without final drift comparison.
- The authoritative document is docs/mcp-probe-kit-delivery-system-v1.md and is now explicitly the final pre-development requirements baseline.
- Tool counts are locked by visibility: current Compact 23 / Compact+Memory 29 / Full 33 / Apps model-visible 29 / app-only 1 / unique names 34; target after architecture is 24 / 30 / 34 / 30 / 1 / 35.
- The document includes a final logical architecture diagram and a 35-row tool matrix covering current implementation path, current effect, target effect, and required changes.
- architecture owns ARC-8 and now has explicit input contract, unified output contract, implementation file layout, mode mapping, gates, and ten acceptance scenarios.
- fix_bug remains the single source of truth for SRC-8; start_bugfix only orchestrates or expands the shared method.
- Managed delivery flows require converge before persisting their MemoryCandidate, while explicit standalone memory administration may call memory tools directly with evidence and applicability.
- Plan/Heartbeat/Resume/Converge are required for managed long-running or formal delivery workflows, not for all direct read-only tools.
- Implementation is frozen into reversible phases: baseline; responsibility alignment; architecture; plan state/convergence; orchestrator closure; diff/verification consistency; memory quality.
- Exact stop/rollback conditions are part of the requirements, including unexpected tool counts, app-only leakage, host visibility changes, protocol regressions, project_root changes, undeclared writes, and overly coupled changes.
- Phase 0 is implemented as a machine-readable compatibility contract rather than a prose-only snapshot.
- The baseline stores capability groups once and derives Compact, Compact+Memory, Full, Apps model-visible, App-only and unique-callable surfaces to avoid duplicated name lists.
- The existing audit:tools command now runs focused baseline validator tests and exact runtime set comparisons.
- Surface comparison is set-based rather than order-based, because tool order is not a public compatibility contract.
- App-only actions are checked both for hidden model visibility and direct negotiated callability.
- Implementation status is tracked separately from the frozen requirements document.
- Skill and Catalog now distinguish direct domain/atomic capability calls from complete-delivery start_* orchestration.
- workflow remains an optional first-tool fallback and does not execute tools or own task lifecycle.
- fix_bug is described as the direct SRC-8 capability; start_bugfix is the complete-delivery orchestrator that composes or expands the same method.
- Plan, Heartbeat and Converge guidance applies to managed, resumable or formal delivery tasks rather than every single read-only call.
- Managed workflow MemoryCandidates require convergence before persistence, while explicit standalone Memory management remains directly callable.
- Critical existing requirements were preserved: complete start_feature description, spec_layout=auto, parent-child support, first heartbeat full plan, and convergence evidence terms.
- Phase 1 changes responsibility guidance, generated Skill semantics and regression tests only; no public schemas, handlers, tool names or visibility changed.
- ARC-8 is implemented in one shared architecture-method core rather than duplicated across tool modes or orchestrators.
- assess, design, validate and drift share one normalized result model and one set of gates.
- The first Phase 2 commit does not register or expose architecture; Catalog, Registry, Visibility and frozen tool counts remain unchanged until the handler and integration tests are ready.
- Architecture input accepts concise candidates and normalizes them into complete structured output.
- Design is blocked without current facts, structural causes, protected invariants, alternatives, a decision and target boundaries.
- Data or public contract changes require migration and rollback; drift requires real diff, target design and graph/runtime evidence.
- Drift detection is bounded and labels potential rule violations rather than claiming a complete static architecture scan.
- Expose exactly one new model-visible architecture tool with assess/design/validate/drift modes and ARC-8 as the single shared methodology source.
- Keep architecture directly callable; workflow only offers optional routing for explicit architecture work, while ordinary code understanding remains code_insight.
- Split ARC-8 into architecture-types, architecture-normalization, architecture-validation, and architecture-method so every new production module stays below 500 lines.
- Keep save_to_docs delegated; the MCP tool returns a document plan and does not directly rewrite project architecture documents.
- Update official tool surfaces to Compact 24, Compact+Memory 30, Full 34, Apps model-visible 30, App-only 1, unique callable 35.
- Preserve list_memory_assets as App-only and add no architecture MCP App UI.
- Keep resume_plan read-only; do not infer or fabricate step completion from code changes or conversation text.
- When a workbench finds no checkpoint, create a marked initial checkpoint but keep it in an untracked/reconciliation state until an Agent verifies and writes real progress.
- Require managed Feature and Bug workflows to establish the first heartbeat with the full Plan and write a cumulative heartbeat after every completed, skipped or blocked step.
- Complete start_feature steady Plans with explicit implement, test and code_review steps; keep direct atomic tools unaffected by these managed-workflow rules.
- Resolve and persist projectRoot in declaredScope so all Plan state calls use one stable workspace.
- Commit Phase 3 and the workbench/orchestrator fix separately for reversibility.
- All start_ui modes use one shared DelegatedPlanContract and one generated report; no mode maintains a separate plan truth source.
- The UI delivery plan includes real implementation, desktop/mobile screenshot evidence, visual iteration, state/accessibility acceptance, tests, code review, conditional architecture drift, context update and optional MemoryCandidate preparation.
- Every UI plan step has explicit dependencies so resume_plan restores a real next step rather than relying on array order or prior chat.
- Architecture drift is conditional: required when ArchitectureCandidate/ADR or boundaries/contracts are involved, otherwise the step must be explicitly skipped with a reason.
- start_ui remains a planning/orchestration tool and does not claim Agent file, runtime, screenshot, test or review actions were performed.
- The large start_ui module was split into request, configuration, plan and output modules; all production modules remain below 500 lines.
- start_onboard is a read-only managed workflow with requiredEvidenceKinds=[] and explicit quality gates rather than implementation/test/review evidence requirements.
- The onboarding plan covers project instructions, context, code graph, manifests/runtime configuration, entry points/module/data flows, real command extraction and safe verification, constraints/risks, quickstart/navigation documentation, acceptance and optional project-knowledge MemoryCandidate preparation.
- Commands not actually executed must remain candidates; verification requires command, cwd and exit code, and onboarding never installs dependencies or starts long-running services automatically.
- All onboarding conclusions distinguish fact, inference and unknown and must cite current repository/config/graph/command evidence.
- docs_dir is restricted to a safe project-relative path, and project_root is resolved and pinned in declaredScope.
- All steps have explicit dependencies and use the shared Heartbeat/Resume/Converge protocol.
- start_product uses product-specific required evidence (requirements and review) and quality gates; it does not require production implementation, software tests or code_review.
- Material product gaps become an explicit user-input step and must be closed, accepted as assumptions or recorded as blockers.
- ui_design_system is represented truthfully as returning a structured contract; a separate Agent action persists design-system JSON and Markdown.
- start_ui is a child delegated workflow; the parent html-prototype step is complete only after recording child plan_id, real prototype artifacts, screenshot acceptance and child converge passed=true.
- The product package receives dedicated PRD review, prototype consistency/visual acceptance and final product-package review before parent convergence.
- Memory recall and MemoryCandidate preparation are optional and remain gated by the parent plan.
- docs_dir and requirements_file are contained within the resolved project root.

## 测试结果

- npx vitest --run src/lib/__tests__/dev-workflow.unit.test.ts src/server/__tests__/tool-registry.unit.test.ts src/protocol/__tests__/mcp-apps.integration.test.ts src/lib/__tests__/workflow-skill-installer.unit.test.ts --testTimeout=15000：4 个文件、37 项通过。
- npm run audit:tools：失败，Apps tools/list missing list_memory_assets。
- npm run release:static：通过，37 项静态发布检查通过。
- npm run docs:verify：通过，1067 项检查、4 种语言、34 个工具条目、5 个动态演示、6 个动画资源、8 份 README。
- npm run build：通过，fresh build/index.js 已生成。
- npm test：98 files / 463 tests 全部通过。
- npm run release:static：37 checks，0 errors/0 warnings；docs 1067 checks 通过。
- npm run audit:tools：38/38 calls 通过；23/29/33/29 工具面保持。
- npm run smoke:protocol：Legacy、Modern、full compatibility 全部通过。
- Claude project-routing：5/5；code-git：6/6；development-orchestration：4/4；ui-product：4/4；ralph-plan-lifecycle：4/4；memory-crud：6/6；full-compatibility：4/4。总计 33/33，0 assessmentFailures。
- git diff --check：通过。
- npm run docs:verify：1067 项通过。
- git diff --check -- docs/engineering-control-plane-v1.md：通过。
- history_session_validate：1-7 编号、映射和索引有效。
- npm run docs:verify：1067 项通过，toolEntries=34。
- git diff --check -- docs/mcp-probe-kit-delivery-system-v1.md passed.
- npm run docs:verify passed: 1067 checks, 34 tool entries.
- git diff --check passed for the design document.
- npm run audit:tools passed: 38/38; compact 23; compact+Memory 29; full 33; appsRaw 29.
- history_session_validate passed with valid sequence and index.
- npm run docs:verify passed: 1067 checks, 34 documented tool entries.
- npm run docs:verify passed: 1067 checks, 34 documented model-visible tool entries.
- git diff --check passed.
- Tool-surface baseline validator tests passed 4/4.
- npm run audit:tools passed 38/38 runtime contract calls and exact surface checks.
- npm run smoke:protocol passed for legacy 23, modern 23 and full 33 surfaces.
- npm run docs:verify passed 1067 checks.
- Focused Skill, workflow and registry tests passed 42/42.
- Deterministic Agent governance evals passed 2/2.
- Full regression suite passed 464/464 across 98 files.
- Tool-surface validator tests passed 4/4 and runtime contract audit passed 38/38.
- Dual-era protocol smoke passed: legacy 23, modern 23, full 33.
- Workflow Skill verification passed with 33 tools synchronized.
- Documentation verification passed 1067 checks.
- ARC-8 methodology unit tests passed 7/7.
- npx tsc --noEmit passed after correcting the alternative input type.
- npx tsc --noEmit passed.
- Full npm test passed: 100 test files, 477 tests.
- npm run build passed.
- npm run audit:tools passed: 39/39 calls; Compact 24; Compact+Memory 30; Full 34; App-only 1; unique callable 35.
- npm run smoke:protocol passed for Legacy and Modern; Full surface 34.
- npm run release:static passed: 37 readiness checks, 0 errors, 0 warnings; docs verification 1080 checks passed.
- ARC-8 method/tool focused tests passed: 11/11.
- Focused Plan metadata, workbench, Feature and Bugfix tests passed.
- Full regression passed: 102 test files, 492 tests, 0 failures.
- TypeScript noEmit check passed.
- Production build passed and synchronized 34 tools.
- Tool contract audit passed: 39/39.
- Legacy and Modern protocol smoke passed; Modern Plan state stored and converged.
- Release static verification passed: 37/37 checks, 0 errors, 0 warnings.
- Docs verification passed: 1080 checks across zh-CN, en, ja and ko.
- git diff --check passed; only expected LF-to-CRLF warnings were emitted.
- start_ui focused tests passed: 48/48 across 4 files.
- Full regression passed: 496/496 across 102 test files.
- npm run audit:tools passed: 39/39; surfaces remain 24/30/34/30/1/35.
- npm run smoke:protocol passed for Legacy, Modern and full compatibility surfaces.
- npm run release:static passed: 37/37 release checks and 1080 docs checks.
- start_onboard focused tests passed: 3/3.
- Full regression passed: 498/498 across 102 test files.
- start_product focused tests passed: 5/5.
- Full regression passed: 501/501 across 102 test files.

## 当前运行状态

- workspace=E:\workspace\github\mcp-probe-kit
- branch=develop/v4
- HEAD=e8bc11ab0ba15e19226d7a0a6f924274ecadf123
- origin/develop/v4=03bd6812ed478b5340cdd0eaaaf666c31ea3e920
- package=4.0.0-rc.8
- worktree 未跟踪项：CLAUDE.md、docs/history-session/
- base HEAD=e8bc11ab0ba15e19226d7a0a6f924274ecadf123
- Node.js v22.21.1
- Claude Code 2.1.179
- 本地 build/index.js fresh build
- 未 push、tag 或 publish
- HEAD=9c34bb584d1f09e0c54ce674497440abb18006fe
- 本轮仅新增正式设计文档和跟踪例外，未修改生产代码。
- HEAD before this design correction=a578da3
- 本轮只修订设计文档，不修改生产代码。
- HEAD=a653801f（本轮提交前）
- 仅修改需求与历史文档，未修改生产代码。
- HEAD=b5bb557（提交前）
- 本轮只修订最终需求文档，尚未实现 architecture 生产代码。
- Branch develop/v4.
- Phase 3 commit: f137704 feat: make plan convergence evidence-driven.
- First Phase 4/workbench fix commit: 39a4866 fix: synchronize workbench plan progress.
- No push, tag, release or package publication performed.
- Working tree should contain only the progress/history documentation update plus unrelated untracked CLAUDE.md and history-session support files before the final documentation commit.

## 剩余问题

- 当前工具契约审计失败，分支不具备完整 release:verify 条件。
- scripts/inspector-smoke.mjs、README.md、docs/specs/mcp-v4/compatibility-matrix.md 等仍有旧 App-only 语义，需要统一。
- 路由冲突缺少属性测试和显式冲突处理。
- bootstrap 写盘副作用及异常吞并需要重新划分边界。
- 本地提交尚未 push，远端与本地运行版本可能不一致。
- 聚合 Claude 审计在当前 10 分钟命令上限内无法结束且缺少逐批进度；全覆盖已通过 7 个有界批次验证。后续可单独改进审计并发、进度与每批超时，但不应混入本次功能契约修复。
- 未执行完整 release:verify 中的包安装、回滚、Inspector、稳定性 soak、安全审计和跨平台 GitNexus；本次范围是契约修复与本地全功能 Agent 验收。
- 尚未形成正式的全局产品/架构设计文档和阶段任务清单。
- 现有图谱数据结构与架构规则、数据所有权、契约和测试覆盖之间尚未建立统一模型。
- 现有 workflow 仍以场景路由为主，尚不能可靠输出任务风险等级和强制闸门。
- 现有 start_* 指导链可被 Agent 跳过，缺少真正由状态和证据驱动的执行许可。
- 设计尚未进入代码实施，TaskProfile、TaskRiskClassifier、ProjectSnapshot、GateEngine、Permit 和扩展 Evidence Schema 均未实现。
- 需要在实施前把 Phase 0 行为基线和 100 个真实分类样本固定下来。
- 需要对设计中的阈值、硬升级规则和 Risk Waiver 进行真实项目校准，避免过度治理。
- ToolInvocationGateway/PolicyKernel 目前仅完成设计修订，尚未实现。
- 需要在 Phase 1 前明确哪些工具属于受治理调用、哪些纯只读工具可以无 Task Session 使用。
- 需要设计 Task Session 如何在 project_root、plan_id 和 Host 会话之间稳定关联。
- 现有设计文档仍写明 TaskRiskClassifier 为最终等级判定者，需要修订后再进入实现。
- 需要明确 Agent 评估失败、过度乐观或绕过风险说明时，MCP 如何要求补证据而不是自行武断分类。
- 需要定义哪些客观事实可以自动触发最低控制要求，避免指导架构退化为无约束建议。
- 尚需确定自动召回的触发条件、结果数量和相关性阈值，避免记忆注入过多干扰 Agent。
- 尚需定义记忆冲突、版本适用范围、项目级与全局级作用域的最小可行规则。
- 最终需求尚未进入 Phase 0/Phase 1 实施。
- 需要按最终需求核对当前 Catalog、Skill、start_* Plan 和 Memory 流程的差异，形成最小子任务清单。
- 需要保证后续实现不再引入自动分级、重型控制面或新增公开工具。
- architecture 工具尚未实现，当前生产工具面仍是现有兼容基线。
- 需要在 Phase 0 后单独设计 architecture 输入输出 Schema、Tool Catalog、Visibility、Registry、测试和 Host/CLI 验收。
- 最终需求文档修订需要独立提交。
- The formal docs are corrected, but production Catalog/Skill wording and tests still need alignment.
- No production code has been changed in this correction.
- Production descriptions and tests are not yet aligned with this corrected invocation model.
- No production code changed in this correction.
- The architecture tool is still a target capability, not yet implemented in production.
- The design document count must explicitly distinguish model-visible count from total unique callable names.
- No production code was changed in this turn.
- The final architecture tool input/output schema and visibility policy must be locked by the acceptance response before implementation.
- ARC-8 is now specified in design docs but not implemented in production.
- Architecture unit and integration acceptance scenarios still need to be written before coding.
- architecture, enhanced Plan/Converge, orchestrator closure, and diff comparison remain design-only and are not implemented.
- The final requirements document is Markdown in the repository; no separate DOCX was requested or generated.
- Phase 1 responsibility wording is still inconsistent in workflow skill guidance and common flow examples.
- The production architecture tool and ARC-8 core do not exist yet.
- Phase 0 baseline must be intentionally updated in the same isolated commit that exposes architecture during Phase 2.
- The architecture tool and ARC-8 production core are not implemented yet.
- Orchestrator completion loops, Plan lifecycle extensions, real diff comparison and Memory quality work remain in later phases.
- The public architecture tool is not yet registered or callable.
- Automatic project evidence collection and Memory recall are not yet connected to the tool adapter.
- Tool-surface target counts remain unchanged until the exposure commit.
- Phase 3 Plan lifecycle enhancements are not implemented yet.
- start_* complete-delivery closure and real diff/architecture drift integration remain Phase 4 and Phase 5 work.
- No push, tag, release, or package publication has been performed.
- Existing historical executions that never called plan_heartbeat cannot be automatically reconstructed with certainty; the fixed workbench marks them untracked and requires Agent reconciliation.
- Phase 4 remains incomplete for start_ui, start_onboard, start_product and start_ralph.
- Phase 5 real diff, contract and architecture drift consistency remains pending.
- Phase 6 Memory conflict, supersede, expiry and retrieval-quality improvements remain pending.
- start_onboard still primarily orchestrates init_project_context and lacks complete project mental-model, command, graph, Memory and formal convergence closure.
- start_product and start_ralph remain incomplete Phase 4 workflows.
- Phase 5 diff/verification consistency and Phase 6 Memory quality remain pending.
- start_product still lacks complete product artifact acceptance, state recovery and formal convergence closure.
- start_ralph still lacks unified Plan, per-round heartbeat evidence, bounded stop/failure policy and final convergence.
- start_ralph remains the only incomplete Phase 4 orchestrator.

## 下一步

- 先修复当前 list_memory_assets 契约冲突，并让 audit、integration、Inspector、README、compatibility matrix 共用同一生成源。
- 新增 contract:verify 作为每次边界改动必须通过的快速闸门，并纳入 release:static。
- 设计 ToolContract 与 ExecutionContext 的渐进式重构，不重写核心工具实现。
- 重构 workflow 路由为可解释的决策表；多规则冲突时返回 unknown/ask_user 或组合计划，不再靠数组顺序。
- 增加运行指纹：版本、commit、build 时间、入口路径、projectRoot、协议和 Host 能力，便于确认实际加载的是哪一份 build。
- 提交本次最小修复与 history checkpoint，保持可单独回滚。
- 后续架构健康治理先建立行为基线和依赖/契约漂移报告，再逐项解耦，不进行一次性重写。
- 形成 MCP Probe Kit 全能力地图：现有能力、重复能力、缺失能力、强制性不足和可删除能力。
- 设计统一交付状态机：Intake→Understand→Baseline→Design→Spec→Plan→Implement→Verify→Drift Check→Converge→Learn。
- 定义 Project Digital Twin 最小数据模型及图谱适配层，明确代码、架构、数据、契约、运行时、测试和历史各自的事实来源与置信度。
- 设计任务分级与策略矩阵，明确 L0-L3 各自所需闸门、证据、回滚和 Claude 本地 build 验收范围。
- 在任何代码修改前输出渐进式实施计划、影响范围、兼容基线和回滚点，用户确认后再开发。
- 评审 docs/engineering-control-plane-v1.md 的核心决策，特别是分类权责、L0-L3 矩阵和公开工具面策略。
- 用户确认后先进入 Phase 0：冻结工具面、Host、协议、Plan、Memory、Graph、Claude 验收基线，不改生产行为。
- 随后以观察模式实现 TaskProfile/TaskRiskClassifier，仅报告等级，不阻断现有流程。
- 在 Phase 0 能力基线中补充当前所有工具的治理分类：free-read、task-entry、state-transition、convergence、destructive。
- 设计 Gateway 的最小输入输出契约和兼容接入点，确保不改变现有工具数量和 handler 行为。
- 以观察模式验证直接 start_* 与 workflow→start_* 的分类一致性。
- 修改 Engineering Control Plane V1 设计稿，将服务端最终分类改为 Agent-led assessment + MCP policy validation。
- 重新设计 TaskAssessment Schema、requiredControls、missingEvidence 和客观风险触发规则。
- 移除对 workflow 或 TaskRiskClassifier 自动决定 L0-L3 的依赖，保留等级作为展示和统计字段。
- 用实际项目案例验证：同一需求由不同 Agent 评估时，MCP 是否能通过统一证据与控制要求收敛到一致工程流程。
- 重新整理最终最小架构，将 Memory Recall、Candidate、Validation、Persist、Maintenance 明确嵌入 start_*、plan_heartbeat、converge 和后续任务。
- 后续修订设计文档时删除过度自动分类部分，但保留并强化 Memory 生命周期和跨 Agent 复用机制。
- 提交最终需求基线文档。
- 下一阶段先完成 Phase 0 兼容行为清单和 Phase 1 职责/流程差异清单，不直接改生产功能。
- 用户确认后再拆分可回滚子任务并逐项实施。
- 提交本次最终需求纠偏。
- 进入 Phase 0 时先冻结当前 SRC-8、工具面、Memory、Plan、Graph 和验证行为。
- 随后以独立阶段实现单一 architecture 工具，不混入其他架构重构。
- Before production changes, produce a bounded gap list showing where Catalog, Skill, schemas, tests, and start_bugfix still blur orchestration versus capability ownership.
- Do not redesign SRC-8; consolidate all definitions under the existing fix_bug/src8 single source of truth.
- Before implementation, audit Skill and Catalog wording for any implication that all atomic tools require a start_* workflow.
- Keep start_* plans selective and scenario-specific; do not create universal tool chains.
- Use this diagram as the authoritative structure before producing a concrete implementation gap list.
- User reviews and accepts the complete tool matrix and acceptance checklist.
- After acceptance, implement in reversible phases: architecture tool; shared Plan enhancements; orchestrator closure; code_review diff/plan comparison; Skill/Catalog/visibility alignment; full protocol/CLI/Host/Agent validation.
- Update the final tool acceptance matrix so architecture explicitly includes ARC-8 inputs, outputs, gates, mode mapping, and tests.
- After user acceptance, implement ARC-8 from one shared architecture core and keep orchestrators thin.
- User reviews and signs off the section 15.9 development-start checklist.
- After acceptance, begin Phase 0 baseline capture and Phase 1 responsibility alignment; do not implement architecture before the baseline is preserved.
- Commit Phase 0 independently.
- Start Phase 1 by aligning Skill, Catalog, workflow guidance and tests around optional routing, start_* orchestration and direct atomic capability calls without changing public schemas.
- Do not expose architecture until its shared ARC-8 core, schemas and tests are ready in Phase 2.
- Commit Phase 1 independently.
- Begin Phase 2 by implementing the shared ARC-8 architecture methodology core, input/output schemas and unit tests before exposing the public architecture tool.
- Update the frozen tool-surface baseline only in the isolated commit that actually exposes architecture after its contracts pass.
- Commit the unexposed ARC-8 foundation independently.
- Implement architecture.ts as a thin adapter over architecture-method, with truthful guidance and optional delegated document plan.
- Add Schema/Catalog/Registry/Output/Visibility integration and update the frozen surface baseline only in the isolated exposure commit.
- Run unit, tool registry, CLI, protocol, audit and full regression tests before marking Phase 2 complete.
- Commit Phase 2 independently without push.
- Begin Phase 3 with backward-compatible Plan state fields, plan-driven evidence requirements, Heartbeat/Resume preservation, and Converge evaluation changes.
- Commit the implementation progress and updated history checkpoint without including unrelated untracked files.
- Continue Phase 4 with one bounded orchestrator at a time, starting by auditing start_ui Plan contract and convergence closure.
- For each later workflow, preserve direct atomic tool usage and apply Plan/Heartbeat/Converge only to managed complete-delivery flows.
- Commit the start_ui Phase 4 slice independently without push.
- Continue Phase 4 with a bounded audit and closure of start_onboard.
- After onboard, close start_product and start_ralph independently.
- Commit the start_onboard Phase 4 slice independently without push.
- Continue Phase 4 with start_product, preserving product-delivery evidence semantics rather than requiring code implementation.
- Close start_ralph independently, then run the final Phase 4 regression.
- Commit the start_product Phase 4 slice independently without push.
- Close start_ralph with a formal bounded-round plan, per-round heartbeat evidence, stop/failure policy and final convergence.
- Run the final Phase 4 regression and mark Phase 4 complete.

## 本轮检查点

### auto-f3e5d68dcc89e60f

```json
{
  "turn_id": "auto-f3e5d68dcc89e60f",
  "timestamp": "unix:1785804213",
  "user_intent": "读取既有开发历史并诊断 MCP Probe Kit 为何持续出现新错误，判断是否存在架构层问题。",
  "findings": [
    "问题属于控制面/契约架构缺陷，不是核心运行时仍为单体。src/index.ts 已约 4KB，Tool Registry 也已模块化。",
    "当前存在可复现的契约冲突：生产代码与 MCP Apps 集成测试要求 list_memory_assets 不进入 tools/list，但 scripts/tool-contract-audit.mjs 仍断言它必须出现在 tools/list。",
    "npm run audit:tools 当前失败，错误为 Apps tools/list missing list_memory_assets；而 4 个相关定向测试 37 项全部通过，npm run release:static 及 docs:verify 1067 项也通过，证明各闸门维护了不同的事实模型。",
    "README、compatibility-matrix、inspector-smoke、历史文档中仍保留旧的 raw tools/list 24/30 与 list_memory_assets 可出现的约定，和当前运行时语义不一致。",
    "src/lib/dev-workflow-routing.ts 仍以重叠正则、数组顺序和分数平局决定路由；例如同时含“错误”和“架构”的意图会同时命中 bugfix/explore，并因规则顺序偏向 bugfix。",
    "Tool Catalog、输入 Schema、独立 Output Schema 注册表、App-only Schema、可见性、发布审计和文档仍分散维护，单一事实源只完成了一部分。",
    "任意普通工具调用前仍会执行带写盘副作用的 bootstrap；bootstrap 异常被记录后返回 null，工具继续执行，可能造成部分初始化和隐性状态差异。",
    "本地 develop/v4 的 HEAD 为 e8bc11a，origin/develop/v4 仍在 03bd681；本地约有 10 个修复提交尚未进入远端，叠加本地 build、npm 包、CLI fallback 多运行入口，存在版本身份漂移风险。"
  ],
  "decisions": [
    "本轮只做架构诊断和复现，不修改生产代码。",
    "下一阶段应暂停继续叠加局部正则和兼容分支，先收敛控制面契约。",
    "需要建立统一 ToolContract/CapabilityManifest，并由其生成 tools/list、App-only 行为、审计期望、文档计数和兼容矩阵。",
    "需要把项目根目录、宿主能力、协议时代、Memory 配置和运行构建身份解析为一次性不可变 ExecutionContext，验证通过后再执行 bootstrap 或外部 I/O。"
  ],
  "files_changed": [],
  "tests": [
    "npx vitest --run src/lib/__tests__/dev-workflow.unit.test.ts src/server/__tests__/tool-registry.unit.test.ts src/protocol/__tests__/mcp-apps.integration.test.ts src/lib/__tests__/workflow-skill-installer.unit.test.ts --testTimeout=15000：4 个文件、37 项通过。",
    "npm run audit:tools：失败，Apps tools/list missing list_memory_assets。",
    "npm run release:static：通过，37 项静态发布检查通过。",
    "npm run docs:verify：通过，1067 项检查、4 种语言、34 个工具条目、5 个动态演示、6 个动画资源、8 份 README。"
  ],
  "runtime_state": [
    "workspace=E:\\workspace\\github\\mcp-probe-kit",
    "branch=develop/v4",
    "HEAD=e8bc11ab0ba15e19226d7a0a6f924274ecadf123",
    "origin/develop/v4=03bd6812ed478b5340cdd0eaaaf666c31ea3e920",
    "package=4.0.0-rc.8",
    "worktree 未跟踪项：CLAUDE.md、docs/history-session/"
  ],
  "remaining_issues": [
    "当前工具契约审计失败，分支不具备完整 release:verify 条件。",
    "scripts/inspector-smoke.mjs、README.md、docs/specs/mcp-v4/compatibility-matrix.md 等仍有旧 App-only 语义，需要统一。",
    "路由冲突缺少属性测试和显式冲突处理。",
    "bootstrap 写盘副作用及异常吞并需要重新划分边界。",
    "本地提交尚未 push，远端与本地运行版本可能不一致。"
  ],
  "next_actions": [
    "先修复当前 list_memory_assets 契约冲突，并让 audit、integration、Inspector、README、compatibility matrix 共用同一生成源。",
    "新增 contract:verify 作为每次边界改动必须通过的快速闸门，并纳入 release:static。",
    "设计 ToolContract 与 ExecutionContext 的渐进式重构，不重写核心工具实现。",
    "重构 workflow 路由为可解释的决策表；多规则冲突时返回 unknown/ask_user 或组合计划，不再靠数组顺序。",
    "增加运行指纹：版本、commit、build 时间、入口路径、projectRoot、协议和 Host 能力，便于确认实际加载的是哪一份 build。"
  ],
  "notes": "本轮未修改任何项目文件。"
}
```

### auto-56ef2275eb3120c9

```json
{
  "turn_id": "auto-56ef2275eb3120c9",
  "timestamp": "unix:1785806207",
  "user_intent": "以最小范围修复工具契约审计与 App-only 可见性行为不一致，并使用本地新编译版本通过自动化、协议和 Claude 全功能验收，确保现有功能不退化。",
  "findings": [
    "生产代码与 MCP Apps 集成测试均要求 list_memory_assets 不进入 tools/list；失败来自 scripts/tool-contract-audit.mjs 的旧断言。",
    "本批未修改生产运行时、Tool Registry、MCP Apps 注册、Memory 实现、路由或 Bootstrap。",
    "新编译 build/index.js 的 deterministic audit 38/38 通过，工具面保持 compact=23、compact+Memory=29、full=33、Apps raw=29。",
    "Claude Code 2.1.179 按 7 个有界批次真实调用本地 build/index.js，33/33 模型可见工具全部通过，0 assessment failure，missing/unexpected 均为空。",
    "单次聚合 Claude 审计超过当前执行器 10 分钟上限且无中间进度；拆分批次后全部完成，属于测试执行可观测性/时长问题，不是功能失败。"
  ],
  "decisions": [
    "仅修复 scripts/tool-contract-audit.mjs 的过期契约，不进行架构重构。",
    "list_memory_assets 继续保持 tools/list 隐藏，但在已协商 MCP Apps 后允许按已知名称调用。",
    "任何后续架构治理必须独立立项，先锁定行为基线和兼容矩阵，再做小步可回滚改动。",
    "本地验证完成后只做本地提交，不 push、tag 或 publish。"
  ],
  "files_changed": [
    "scripts/tool-contract-audit.mjs",
    "docs/history-session/7.md"
  ],
  "tests": [
    "npm run build：通过，fresh build/index.js 已生成。",
    "npm test：98 files / 463 tests 全部通过。",
    "npm run release:static：37 checks，0 errors/0 warnings；docs 1067 checks 通过。",
    "npm run audit:tools：38/38 calls 通过；23/29/33/29 工具面保持。",
    "npm run smoke:protocol：Legacy、Modern、full compatibility 全部通过。",
    "Claude project-routing：5/5；code-git：6/6；development-orchestration：4/4；ui-product：4/4；ralph-plan-lifecycle：4/4；memory-crud：6/6；full-compatibility：4/4。总计 33/33，0 assessmentFailures。"
  ],
  "runtime_state": [
    "branch=develop/v4",
    "base HEAD=e8bc11ab0ba15e19226d7a0a6f924274ecadf123",
    "Node.js v22.21.1",
    "Claude Code 2.1.179",
    "本地 build/index.js fresh build",
    "未 push、tag 或 publish"
  ],
  "remaining_issues": [
    "聚合 Claude 审计在当前 10 分钟命令上限内无法结束且缺少逐批进度；全覆盖已通过 7 个有界批次验证。后续可单独改进审计并发、进度与每批超时，但不应混入本次功能契约修复。",
    "未执行完整 release:verify 中的包安装、回滚、Inspector、稳定性 soak、安全审计和跨平台 GitNexus；本次范围是契约修复与本地全功能 Agent 验收。"
  ],
  "next_actions": [
    "提交本次最小修复与 history checkpoint，保持可单独回滚。",
    "后续架构健康治理先建立行为基线和依赖/契约漂移报告，再逐项解耦，不进行一次性重写。"
  ],
  "notes": "本批以最小修改恢复审计一致性，没有改变任何生产功能或架构边界。"
}
```

### auto-45e3fb025147cd34

```json
{
  "turn_id": "auto-45e3fb025147cd34",
  "timestamp": "unix:1785807904",
  "user_intent": "全面反思 MCP Probe Kit 当前能力缺口，避免继续按用户逐条提醒才局部补功能，并重新定义如何发挥 MCP 的最大价值。",
  "findings": [
    "此前分析方式过于问题驱动和局部修复，缺少从完整软件交付生命周期、Agent 行为约束和产品护城河角度进行能力地图与缺口分析。",
    "当前项目已有 workflow、start_*、code_insight/GitNexus、Spec、plan_heartbeat、converge、Memory、Claude 验收等组件，但它们仍主要是松散工具和建议链，缺少统一的权威项目模型、任务风险分级、强制生命周期状态机、架构与数据所有权闸门以及修改前后漂移验证。",
    "start_feature 已接入任务级图谱和 graph-insights 基线，但图谱目前更多用于提供上下文线索，没有成为架构规则、数据所有权、公共契约、测试覆盖和变更影响的统一证据底座。",
    "MCP Probe Kit 最大价值不应是替 Agent 生成更多代码，而应成为跨宿主、跨 Agent 的软件工程控制面：让任何 Agent 都按可恢复、可验证、可审计、可维护的方式交付软件。",
    "核心缺口应统一定义为 Engineering Control Plane 与 Project Digital Twin，而不是继续零散增加 start_architecture、check_architecture 等孤立工具。"
  ],
  "decisions": [
    "先做完整能力地图和目标产品架构，再决定是否增加公开工具；避免通过增加大量工具名称掩盖编排与治理缺失。",
    "以 Project Digital Twin 作为统一事实模型，整合代码图谱、模块边界、数据所有权、公共契约、运行时路径、Spec、测试、Git/history 和 Memory。",
    "以生命周期状态机和策略引擎决定不同任务等级必须经过的上下文、架构、规格、实现、验证、漂移检查和收敛闸门。",
    "公开工具面保持克制，优先增强 workflow、start_*、code_insight 和 converge 的内部协作；内部服务按 ProjectModel、ImpactAnalyzer、ArchitecturePolicy、GateEngine、EvidenceStore 解耦。",
    "实施采用只读基线与报告优先、L2/L3 渐进启用闸门、验证等价后再收口旧逻辑的方式，禁止一次性大重构。"
  ],
  "files_changed": [
    "docs/history-session/7.md"
  ],
  "tests": [],
  "runtime_state": [],
  "remaining_issues": [
    "尚未形成正式的全局产品/架构设计文档和阶段任务清单。",
    "现有图谱数据结构与架构规则、数据所有权、契约和测试覆盖之间尚未建立统一模型。",
    "现有 workflow 仍以场景路由为主，尚不能可靠输出任务风险等级和强制闸门。",
    "现有 start_* 指导链可被 Agent 跳过，缺少真正由状态和证据驱动的执行许可。"
  ],
  "next_actions": [
    "形成 MCP Probe Kit 全能力地图：现有能力、重复能力、缺失能力、强制性不足和可删除能力。",
    "设计统一交付状态机：Intake→Understand→Baseline→Design→Spec→Plan→Implement→Verify→Drift Check→Converge→Learn。",
    "定义 Project Digital Twin 最小数据模型及图谱适配层，明确代码、架构、数据、契约、运行时、测试和历史各自的事实来源与置信度。",
    "设计任务分级与策略矩阵，明确 L0-L3 各自所需闸门、证据、回滚和 Claude 本地 build 验收范围。",
    "在任何代码修改前输出渐进式实施计划、影响范围、兼容基线和回滚点，用户确认后再开发。"
  ],
  "notes": "本轮只进行系统性诊断和产品定位纠偏，不修改生产代码。"
}
```

### auto-3551f3ace34914bd

```json
{
  "turn_id": "auto-3551f3ace34914bd",
  "timestamp": "unix:1785808949",
  "user_intent": "完整设计 MCP Probe Kit Engineering Control Plane V1，明确任务等级由谁判断、Agent 如何执行，以及如何以实际工程问题为中心整合图谱、计划、证据、闸门和跨 Host 一致性。",
  "findings": [
    "现有 workflow 主要做任务类型路由，缺少任务风险等级、分阶段重判和 implementationAllowed 状态。",
    "现有 start_feature 已有任务级图谱和 graph-insights 基线，但图谱目前主要作为提示线索，尚未成为任务分类、数据所有权、公共契约、测试覆盖和漂移检查的统一事实模型。",
    "现有 Plan Evidence 固定为 requirements/spec/implementation/test/review 等少量类型，converge 使用固定证据组，无法按 L0-L3 动态治理。",
    "MCP 无法在所有 IDE 中物理阻止 Agent 直接写文件，因此设计必须区分可强制 Host hook 与不可强制 Host；后者至少做到检测越界、阻断收敛和接管/回滚恢复。",
    "任务类型与风险等级必须分离，同一 feature/bugfix/ui 可分别是 L0-L3。"
  ],
  "decisions": [
    "最终 L0-L3 由服务端 TaskRiskClassifier 判定，不由用户菜单或 Agent 主观决定；模型只提取候选信号。",
    "分类分三次：workflow 快速预判、start_* 基于影响图确认、执行期基于真实 diff 重判。",
    "建立 Project Digital Twin，但 V1 使用版本化快照和任务影响子图，不建设重型中央数据库。",
    "公开工具面第一阶段保持克制，先增强 workflow、code_insight、start_*、plan_heartbeat、resume_plan、converge 的内部协作，不立即增加 architecture 工具。",
    "L0/L1 保持轻量；L2/L3 才强制架构影响、数据所有权、公共契约、漂移和更严格的 Claude 本地 build 验收。",
    "实施采用观察模式→统一快照→Plan/Evidence 升级→警告闸门→强制闸门→Host hook/可视化的渐进路线。"
  ],
  "files_changed": [
    ".gitignore",
    "docs/engineering-control-plane-v1.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "git diff --check：通过。",
    "npm run docs:verify：1067 项通过。"
  ],
  "runtime_state": [
    "branch=develop/v4",
    "HEAD=9c34bb584d1f09e0c54ce674497440abb18006fe",
    "本轮仅新增正式设计文档和跟踪例外，未修改生产代码。"
  ],
  "remaining_issues": [
    "设计尚未进入代码实施，TaskProfile、TaskRiskClassifier、ProjectSnapshot、GateEngine、Permit 和扩展 Evidence Schema 均未实现。",
    "需要在实施前把 Phase 0 行为基线和 100 个真实分类样本固定下来。",
    "需要对设计中的阈值、硬升级规则和 Risk Waiver 进行真实项目校准，避免过度治理。"
  ],
  "next_actions": [
    "评审 docs/engineering-control-plane-v1.md 的核心决策，特别是分类权责、L0-L3 矩阵和公开工具面策略。",
    "用户确认后先进入 Phase 0：冻结工具面、Host、协议、Plan、Memory、Graph、Claude 验收基线，不改生产行为。",
    "随后以观察模式实现 TaskProfile/TaskRiskClassifier，仅报告等级，不阻断现有流程。"
  ],
  "notes": "设计强调实际可执行性：明确谁判断等级、证据不足如何处理、Agent 绕过流程如何接管、不同 Host 能强制到什么程度，以及每阶段的回滚和验收标准。"
}
```

### auto-2097dbfe3fb483e0

```json
{
  "turn_id": "auto-2097dbfe3fb483e0",
  "timestamp": "unix:1785810074",
  "user_intent": "纠正 Engineering Control Plane 设计中过度依赖 workflow 作为主入口的问题，明确真正可靠的统一入口与跨 Host 执行路径。",
  "findings": [
    "当前 workflow 的实现本质上是正则和优先级驱动的场景路由器，适合给出 firstTool，不适合作为风险分类和工程治理的唯一信任边界。",
    "Agent 可能直接调用 start_feature、start_bugfix、refactor 或 CLI fallback；若分类逻辑只挂在 workflow 下，所有直达路径都会绕过统一治理。",
    "公开工具入口与内部治理入口必须分离：公开工具可有多个，内部必须只有一个 ToolInvocationGateway/PolicyKernel。",
    "MCP 不能看到完全绕过 MCP 的外部文件编辑，但可以通过基线 revision、真实 diff、heartbeat 和 converge 检测未规划修改并阻断收敛。"
  ],
  "decisions": [
    "workflow 保留为可选路由助手，不再定义为唯一主入口或分类权威。",
    "新增内部设计概念 ToolInvocationGateway/PolicyKernel，所有相关 MCP 工具和 CLI fallback 在进入 handler 前统一经过任务恢复、快速分类、Gate 检查和运行身份注入。",
    "直接调用 start_* 与先调用 workflow 再 start_* 必须产生相同的 Task Profile、风险等级、Gate 和状态语义。",
    "TaskRiskClassifier 作为 PolicyKernel 的内部服务，不属于 workflow。"
  ],
  "files_changed": [
    "docs/engineering-control-plane-v1.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "git diff --check -- docs/engineering-control-plane-v1.md：通过。",
    "npm run docs:verify：1067 项通过。"
  ],
  "runtime_state": [
    "branch=develop/v4",
    "HEAD before this design correction=a578da3",
    "本轮只修订设计文档，不修改生产代码。"
  ],
  "remaining_issues": [
    "ToolInvocationGateway/PolicyKernel 目前仅完成设计修订，尚未实现。",
    "需要在 Phase 1 前明确哪些工具属于受治理调用、哪些纯只读工具可以无 Task Session 使用。",
    "需要设计 Task Session 如何在 project_root、plan_id 和 Host 会话之间稳定关联。"
  ],
  "next_actions": [
    "在 Phase 0 能力基线中补充当前所有工具的治理分类：free-read、task-entry、state-transition、convergence、destructive。",
    "设计 Gateway 的最小输入输出契约和兼容接入点，确保不改变现有工具数量和 handler 行为。",
    "以观察模式验证直接 start_* 与 workflow→start_* 的分类一致性。"
  ],
  "notes": "用户对 workflow 单点入口的不可靠性判断正确。本次将设计从 workflow-centric 修正为 gateway-centric。"
}
```

### auto-c9575f5ee023e009

```json
{
  "turn_id": "auto-c9575f5ee023e009",
  "timestamp": "unix:1785810381",
  "user_intent": "重新校准 TaskRiskClassifier 的职责边界：用户认为 MCP 无法可靠判断需求层级，应该由 Agent 负责判断，MCP 主要提供指导架构。",
  "findings": [
    "用户指出的核心问题成立：MCP 不能仅凭需求文本可靠判断业务重要性、真实复杂度和风险容忍度，尤其在未理解项目和业务上下文时。",
    "将 TaskRiskClassifier 设为最终权威判定者存在过度自动化风险，也与 MCP Probe Kit 作为指导与工程治理架构的定位冲突。",
    "需求语义判断、业务重要性和方案复杂度更适合由 Agent 结合对话、代码和图谱进行推理；MCP 更适合提供结构化评估框架、强制证据字段、客观工程事实校验和流程一致性。",
    "L0-L3 不应成为驱动流程的单一真相；更可靠的是直接输出 required controls，例如是否需要影响分析、架构设计、Spec、迁移、回滚、全量验证和用户确认。",
    "MCP 可确定性识别公共契约、数据迁移、权限安全、运行入口、跨模块和回滚等客观风险，但不能独立判断战略重要性、SLA、停机容忍度和隐藏业务规则。"
  ],
  "decisions": [
    "采用 Agent-led、MCP-constrained 模式：Agent 提出 Task Assessment 和建议治理深度，MCP 校验证据、一致性和最低强制控制。",
    "将 TaskRiskClassifier 的概念降级为 TaskAssessmentEngine/PolicyAdvisor，不作为最终需求层级裁判。",
    "任务等级改为可选派生标签，核心决策改为 requiredControls 与 missingEvidence；converge 检查证据是否闭环，而不是检查某个等级标签是否正确。",
    "业务关键性、风险容忍度和无法从仓库推断的约束由用户确认；命中客观高风险事实时 MCP 可以自动增加最低控制要求。",
    "高风险任务可要求第二 Agent 或 Claude 独立复核 Assessment，但不让第二模型直接替代事实证据。"
  ],
  "files_changed": [
    "docs/history-session/7.md"
  ],
  "tests": [],
  "runtime_state": [],
  "remaining_issues": [
    "现有设计文档仍写明 TaskRiskClassifier 为最终等级判定者，需要修订后再进入实现。",
    "需要明确 Agent 评估失败、过度乐观或绕过风险说明时，MCP 如何要求补证据而不是自行武断分类。",
    "需要定义哪些客观事实可以自动触发最低控制要求，避免指导架构退化为无约束建议。"
  ],
  "next_actions": [
    "修改 Engineering Control Plane V1 设计稿，将服务端最终分类改为 Agent-led assessment + MCP policy validation。",
    "重新设计 TaskAssessment Schema、requiredControls、missingEvidence 和客观风险触发规则。",
    "移除对 workflow 或 TaskRiskClassifier 自动决定 L0-L3 的依赖，保留等级作为展示和统计字段。",
    "用实际项目案例验证：同一需求由不同 Agent 评估时，MCP 是否能通过统一证据与控制要求收敛到一致工程流程。"
  ],
  "notes": "本轮确认了此前设计中的重要偏差：MCP 的价值是规范 Agent 如何判断和交付，不是替 Agent 理解所有需求并武断划分等级。尚未修改正式设计文档或生产代码。"
}
```

### auto-0c105e8b82b9b790

```json
{
  "turn_id": "auto-0c105e8b82b9b790",
  "timestamp": "unix:1785810782",
  "user_intent": "纠正简化方案中弱化 Memory 的问题，明确长期记忆在统一工程纪律中的必要角色。",
  "findings": [
    "将 Memory 仅称为辅助能力会削弱 MCP Probe Kit 的长期价值；没有跨任务记忆，不同 Agent 仍会重复踩坑、重复探索和重复破坏既有约束。",
    "Memory 的正确位置不是单独堆叠的系统，也不是无条件自动写入，而是贯穿任务前召回、任务中候选、任务后验证沉淀和后续反馈修正。",
    "现有 search_memory、memorize_asset、delete_memory_asset 以及 converge 后写入的纪律应保留并成为最小闭环的一部分。"
  ],
  "decisions": [
    "保留简化后的三层主架构：指导、状态、验证；Memory 作为贯穿三层的第四条纵向能力，而非可有可无的附属功能。",
    "任务开始前按任务意图、模块、错误现象和项目范围自动检索相关记忆；任务执行中只记录候选，不立即写入长期记忆；只有测试、审查和 converge 通过后才正式沉淀。",
    "记忆至少覆盖已验证架构决策、稳定实现模式、历史 Bug 根因与回归保护、Host/协议兼容坑、数据口径与所有权规则、失败方案与禁用做法。",
    "必须支持去重、冲突、过期和删除；新证据可修正或废弃旧记忆，不能把记忆库变成不断累积的垃圾堆。"
  ],
  "files_changed": [
    "docs/history-session/7.md"
  ],
  "tests": [],
  "runtime_state": [],
  "remaining_issues": [
    "尚需确定自动召回的触发条件、结果数量和相关性阈值，避免记忆注入过多干扰 Agent。",
    "尚需定义记忆冲突、版本适用范围、项目级与全局级作用域的最小可行规则。"
  ],
  "next_actions": [
    "重新整理最终最小架构，将 Memory Recall、Candidate、Validation、Persist、Maintenance 明确嵌入 start_*、plan_heartbeat、converge 和后续任务。",
    "后续修订设计文档时删除过度自动分类部分，但保留并强化 Memory 生命周期和跨 Agent 复用机制。"
  ],
  "notes": "本轮仅纠偏设计，不修改生产代码。"
}
```

### auto-d799698478a8eb67

```json
{
  "turn_id": "auto-d799698478a8eb67",
  "timestamp": "unix:1785811307",
  "user_intent": "形成一份不再丢失 Memory、状态、验证或工具组合的完整最终需求，明确现有 33 个工具各自职责、主流程、组合方式和实现顺序。",
  "findings": [
    "用户对前序设计反复摆动和遗漏关键能力不满意，要求锁定一份完整、实际、可执行的最终需求。",
    "现有项目已经具备 33 个工具、start_* 自动记忆注入、Plan heartbeat/resume/converge、图谱和验证能力，主要缺口是职责与组合没有形成一份稳定的最终产品基线。",
    "workflow 适合作为不确定时的路由器，不应成为强制主入口；Agent 负责工程判断，MCP 负责统一方法、项目证据、过程状态、验证和长期记忆。",
    "Memory 必须贯穿 Recall→Candidate→Validate→Converge→Persist→Reuse→Update/Delete 全生命周期，不能被降级为任务末尾可选项。"
  ],
  "decisions": [
    "以 docs/mcp-probe-kit-delivery-system-v1.md 作为后续实施的最终需求基线。",
    "旧 docs/engineering-control-plane-v1.md 标记为已被取代，自动分级、Policy Kernel 和 Permit 不再作为 V1 实施依据。",
    "V1 不新增、不删除、不改名现有 33 个公开工具；Agent 只需记住 start_feature、start_bugfix、start_ui、refactor、start_onboard、workflow 六个主要工程入口。",
    "现有 33 个工具划分为 6 个主要入口、17 个核心执行与记忆工具、10 个可选辅助工具。",
    "统一生命周期确定为 Intake→Recall→Understand→Assess→Specify/Plan→Execute→Verify→Review→Converge→Learn→Commit。",
    "Agent 完成 Task Assessment；MCP 只检查分析是否完整、是否与真实项目事实和 diff 一致，不自动判断需求层级。",
    "入口工具自动召回高相关记忆；执行中通过 plan_heartbeat 保存 MemoryCandidate；只有 converge passed=true 后才允许 memorize_asset。",
    "实施顺序固定为兼容基线→职责和主流程→Task Assessment/MemoryCandidate→Heartbeat/Resume/Converge→Diff验证→Memory质量治理。"
  ],
  "files_changed": [
    ".gitignore",
    "docs/engineering-control-plane-v1.md",
    "docs/mcp-probe-kit-delivery-system-v1.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "git diff --check：通过。",
    "npm run docs:verify：1067 项通过。",
    "history_session_validate：1-7 编号、映射和索引有效。"
  ],
  "runtime_state": [
    "branch=develop/v4",
    "HEAD=a653801f（本轮提交前）",
    "仅修改需求与历史文档，未修改生产代码。"
  ],
  "remaining_issues": [
    "最终需求尚未进入 Phase 0/Phase 1 实施。",
    "需要按最终需求核对当前 Catalog、Skill、start_* Plan 和 Memory 流程的差异，形成最小子任务清单。",
    "需要保证后续实现不再引入自动分级、重型控制面或新增公开工具。"
  ],
  "next_actions": [
    "提交最终需求基线文档。",
    "下一阶段先完成 Phase 0 兼容行为清单和 Phase 1 职责/流程差异清单，不直接改生产功能。",
    "用户确认后再拆分可回滚子任务并逐项实施。"
  ],
  "notes": "本轮的目标是锁定完整产品需求而不是继续扩展概念。最终文档逐一说明 33 个工具的角色、六类主要场景组合、Memory 闭环、异常降级、实施阶段和验收指标。"
}
```

### auto-240039ece83465f8

```json
{
  "turn_id": "auto-240039ece83465f8",
  "timestamp": "unix:1785811777",
  "user_intent": "纠正最终需求中遗漏的 start_bugfix SRC-8 八步法和架构工具，确保两者进入正式产品基线并与 Memory、Plan、验证和 Converge 组合。",
  "findings": [
    "现有代码已经完整实现 SRC-8：明确差距、收敛边界、验收契约、把握真因、制定对策、贯彻修复、评价双轨、巩固传播；此前最终需求摘要把它压缩成普通 Bug 流程，属于严重遗漏。",
    "现有 start_bugfix 的 Delegated Plan 本身承载 SRC-8；fix_bug 是辅助指南，并非 compact 工具面中的必经公开调用。",
    "仅使用 code_insight 和 refactor 不能完整覆盖架构评估、目标设计、实施前校验和实施后漂移检查，需要恢复一个统一且克制的 architecture 工具。",
    "架构能力不应拆成多个工具；单个 architecture 工具使用 assess、design、validate、drift 四种 mode，并复用 code_insight、Memory、Plan、Converge。"
  ],
  "decisions": [
    "start_bugfix 必须严格保留 SRC-8，SRC-4 真因工作表闭合前禁止修改代码，连续三次修复失败返回 SRC-2 或 SRC-4。",
    "SRC-8 必须生成回归保护和 MemoryCandidate，只有 converge passed=true 后才能正式 memorize_asset。",
    "目标工具面从兼容基线 33 个增加到 34 个，只新增一个 architecture 工具，不改名或删除现有工具。",
    "architecture 成为第七个主要工程入口，并接入 start_feature、start_bugfix、refactor 和独立架构任务。",
    "使用过 architecture 的任务，在 converge 前必须执行 validate 或 drift；小型局部修改不强制调用架构工具。"
  ],
  "files_changed": [
    "docs/mcp-probe-kit-delivery-system-v1.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "git diff --check：通过。",
    "npm run docs:verify：1067 项通过，toolEntries=34。"
  ],
  "runtime_state": [
    "branch=develop/v4",
    "HEAD=b5bb557（提交前）",
    "本轮只修订最终需求文档，尚未实现 architecture 生产代码。"
  ],
  "remaining_issues": [
    "architecture 工具尚未实现，当前生产工具面仍是现有兼容基线。",
    "需要在 Phase 0 后单独设计 architecture 输入输出 Schema、Tool Catalog、Visibility、Registry、测试和 Host/CLI 验收。",
    "最终需求文档修订需要独立提交。"
  ],
  "next_actions": [
    "提交本次最终需求纠偏。",
    "进入 Phase 0 时先冻结当前 SRC-8、工具面、Memory、Plan、Graph 和验证行为。",
    "随后以独立阶段实现单一 architecture 工具，不混入其他架构重构。"
  ],
  "notes": "本次纠偏明确：不能为了简化而丢掉已经成熟的核心方法论。SRC-8 和架构能力都是产品主干，不是可选装饰。"
}
```

### auto-20b8d270663b79e9

```json
{
  "turn_id": "auto-20b8d270663b79e9",
  "timestamp": "unix:1785812367",
  "user_intent": "Clarify the architecture: start_* tools are workflow orchestrators, not intent recognizers; fix_bug owns SRC-8, while start_bugfix composes the full bugfix delivery flow.",
  "findings": [
    "Current fix_bug.ts already owns SRC-8 artifacts and gates.",
    "Current start_bugfix.ts composes SRC-8 through mergeBugfixOrchestrationPlan, so the code is partly aligned, but docs and public responsibility wording were blurred."
  ],
  "decisions": [
    "workflow remains an optional routing helper only.",
    "All start_* tools are scenario workflow composers that combine atomic/domain capabilities into a Delegated Plan.",
    "fix_bug is the single source of truth for SRC-8 checklists, gates, worksheets, schemas, and child plan.",
    "start_bugfix may flatten the fix_bug child steps into its top-level plan for execution and heartbeat, but must not redefine SRC-8.",
    "architecture remains an atomic domain capability that can be composed by start_feature, start_bugfix, or refactor."
  ],
  "files_changed": [
    "docs/mcp-probe-kit-delivery-system-v1.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "git diff --check -- docs/mcp-probe-kit-delivery-system-v1.md passed.",
    "npm run docs:verify passed: 1067 checks, 34 tool entries."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "The formal docs are corrected, but production Catalog/Skill wording and tests still need alignment.",
    "No production code has been changed in this correction."
  ],
  "next_actions": [
    "Before production changes, produce a bounded gap list showing where Catalog, Skill, schemas, tests, and start_bugfix still blur orchestration versus capability ownership.",
    "Do not redesign SRC-8; consolidate all definitions under the existing fix_bug/src8 single source of truth."
  ],
  "notes": "This correction prevents start_bugfix from becoming a second bugfix methodology implementation and keeps Agent-led selection plus atomic capability composition."
}
```

### auto-fb8b52f681dd7045

```json
{
  "turn_id": "auto-fb8b52f681dd7045",
  "timestamp": "unix:1785812699",
  "user_intent": "Clarify that not every atomic capability belongs in a start_* orchestration; atomic, composable, and actually orchestrated are different concepts.",
  "findings": [
    "The previous wording incorrectly implied that all remaining tools were expected to be invoked through start_* plans.",
    "The design should be modeled by tool role plus invocation mode, not a strict mandatory hierarchy."
  ],
  "decisions": [
    "Atomic tools are independently callable by Agents.",
    "Only some atomic/domain capabilities are composable by start_* workflows.",
    "A capability is 'orchestrated' only when a particular Delegated Plan actually selects it.",
    "start_* is used for complete delivery workflows, not as a mandatory gateway for all tool calls.",
    "Direct calls such as code_insight, fix_bug, check_spec, architecture, gentest, code_review, memory tools, and git_work_report remain first-class normal paths."
  ],
  "files_changed": [
    "docs/mcp-probe-kit-delivery-system-v1.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "git diff --check passed for the design document.",
    "npm run docs:verify passed: 1067 checks, 34 tool entries."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "Production descriptions and tests are not yet aligned with this corrected invocation model.",
    "No production code changed in this correction."
  ],
  "next_actions": [
    "Before implementation, audit Skill and Catalog wording for any implication that all atomic tools require a start_* workflow.",
    "Keep start_* plans selective and scenario-specific; do not create universal tool chains."
  ],
  "notes": "Core distinction: atomic != must be composed; composable != always composed; orchestration is a per-workflow selection."
}
```

### auto-34b527e7926285e1

```json
{
  "turn_id": "auto-34b527e7926285e1",
  "timestamp": "unix:1785812892",
  "user_intent": "Produce the final logical architecture diagram for MCP Probe Kit after clarifying direct atomic usage versus orchestrated start_* flows.",
  "findings": [
    "The final architecture should show two parallel paths: direct atomic invocation and optional orchestrated delivery.",
    "Shared infrastructure should include Skill, project context/graph, Plan Store, Memory Store, Tool Catalog/Registry, MCP/CLI/Host adapters."
  ],
  "decisions": [
    "Agent is the decision maker and chooses tools using Skill plus current context.",
    "Atomic/domain tools are independently callable; orchestration is optional and task-specific.",
    "start_* tools are complete-delivery workflow composers, not intent recognizers and not mandatory parents of atomic tools.",
    "workflow is only an optional first-tool suggestion when Agent is uncertain.",
    "fix_bug owns SRC-8; start_bugfix composes it into a larger delivery flow.",
    "Plan, Converge, and Memory are shared capabilities used where needed, not universal mandatory wrappers.",
    "architecture is an independently callable domain capability and may also be composed into feature, bugfix, or refactor flows."
  ],
  "files_changed": [],
  "tests": [],
  "runtime_state": [],
  "remaining_issues": [
    "The architecture tool is still a target capability, not yet implemented in production."
  ],
  "next_actions": [
    "Use this diagram as the authoritative structure before producing a concrete implementation gap list."
  ],
  "notes": "No repository files changed in this turn."
}
```

### auto-e4b6c19ab89c2258

```json
{
  "turn_id": "auto-e4b6c19ab89c2258",
  "timestamp": "unix:1785813497",
  "user_intent": "Produce the complete pre-development requirements acceptance baseline: every tool, its current implementation, intended effect, change status, final counts, and end-to-end acceptance scenarios.",
  "findings": [
    "Current tool audit passed 38/38 with surfaces compact=23, compactWithMemory=29, full=33, appsRaw=29.",
    "Current unique callable names are 34 because list_memory_assets is app-only and hidden from tools/list.",
    "start_feature currently closes planning/spec/estimate better than implementation/test/review/converge.",
    "start_bugfix already composes shared SRC-8, graph, memory, and spec gate but needs explicit review/convergence closure and responsibility wording alignment.",
    "start_ui has rich visual execution steps but some modes use plain delegated-shaped plans rather than the shared DelegatedPlanContract and lack full test/review/converge closure.",
    "start_onboard currently orchestrates only init_project_context and should be expanded to code insight, commands, memory, and project navigation.",
    "Plan state lacks explicit MemoryCandidate/ArchitectureCandidate fields; converge currently uses fixed default evidence kinds rather than plan-driven workflow-specific gates.",
    "memorize_asset does not hard-enforce converge, which is acceptable for direct/manual memory management; managed workflows must enforce sequencing through their plan."
  ],
  "decisions": [
    "Count model-visible and app-only surfaces separately: current full model surface 33 plus hidden app-only list_memory_assets; target full model surface 34 after adding architecture, while unique callable names become 35 including the app-only action.",
    "workflow is optional routing only; start_* tools are scenario orchestrators; direct/domain tools can be called independently and are composed only when a concrete workflow needs them.",
    "fix_bug is the public owner of SRC-8 semantics, backed by shared src8 core; start_bugfix composes/expands those steps but does not redefine the methodology.",
    "Plan and Converge are required for managed long-running or formal delivery flows, not for every single read-only atomic call.",
    "Memory tools remain directly usable; within managed workflows, MemoryCandidate is persisted only after convergence, while explicit manual memory management remains possible.",
    "architecture is one new direct model-visible domain tool with assess/design/validate/drift modes and no initial MCP App UI."
  ],
  "files_changed": [],
  "tests": [
    "npm run audit:tools passed: 38/38; compact 23; compact+Memory 29; full 33; appsRaw 29.",
    "history_session_validate passed with valid sequence and index."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "The design document count must explicitly distinguish model-visible count from total unique callable names.",
    "No production code was changed in this turn.",
    "The final architecture tool input/output schema and visibility policy must be locked by the acceptance response before implementation."
  ],
  "next_actions": [
    "User reviews and accepts the complete tool matrix and acceptance checklist.",
    "After acceptance, implement in reversible phases: architecture tool; shared Plan enhancements; orchestrator closure; code_review diff/plan comparison; Skill/Catalog/visibility alignment; full protocol/CLI/Host/Agent validation."
  ],
  "notes": "The acceptance response should be treated as the final functional baseline; no TaskRiskClassifier, PolicyKernel, central intent engine, mandatory workflow entry, or mandatory orchestration of every atomic tool."
}
```

### auto-835a302f309bc3aa

```json
{
  "turn_id": "auto-835a302f309bc3aa",
  "timestamp": "unix:1785814073",
  "user_intent": "Correct the final acceptance baseline by adding the missing architecture-specific methodology, not just architecture modes.",
  "findings": [
    "The prior acceptance baseline listed architecture modes and outputs but omitted the actual method that tells the Agent how to perform architecture work.",
    "The existing design document had assess/design/validate/drift and architecture outputs, so ARC-8 can be added without creating more public tools or a central controller."
  ],
  "decisions": [
    "Define one architecture methodology owned by the architecture tool: ARC-8 (Architecture Reasoning & Change 8-step).",
    "Keep assess/design/validate/drift as entry modes mapped onto ARC-8 stages, not as the methodology itself.",
    "ARC-8 stages are: define architecture problem; reconstruct current facts; identify structural root cause and protected invariants; generate alternatives; evaluate tradeoffs and decide; design target architecture and transition; pre/phase validation; drift verification and learning.",
    "architecture is the single public source of truth for ARC-8; start_feature, start_bugfix, and refactor may invoke or reference it but may not copy its steps or gates.",
    "Architecture gates prevent design without current facts, selection without invariants/root cause, approval without tradeoff evidence, data/contract changes without migration and rollback, and formal convergence without final drift comparison."
  ],
  "files_changed": [
    "docs/mcp-probe-kit-delivery-system-v1.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "git diff --check -- docs/mcp-probe-kit-delivery-system-v1.md passed.",
    "npm run docs:verify passed: 1067 checks, 34 documented tool entries."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "ARC-8 is now specified in design docs but not implemented in production.",
    "Architecture unit and integration acceptance scenarios still need to be written before coding."
  ],
  "next_actions": [
    "Update the final tool acceptance matrix so architecture explicitly includes ARC-8 inputs, outputs, gates, mode mapping, and tests.",
    "After user acceptance, implement ARC-8 from one shared architecture core and keep orchestrators thin."
  ],
  "notes": "Do not treat the four architecture modes as the method. The method is ARC-8; modes are bounded entry points into the shared method."
}
```

### auto-be1971d665e22f26

```json
{
  "turn_id": "auto-be1971d665e22f26",
  "timestamp": "unix:1785814908",
  "user_intent": "Merge all settled design decisions into one complete, detailed, authoritative requirements and acceptance document before development starts.",
  "findings": [
    "Current code audit baseline remains 38/38 with compact=23, compactWithMemory=29, full=33, appsRaw=29.",
    "The previous document was already structurally strong but lacked the full implementation matrix, exact visibility counts, architecture IO/tests, direct-vs-managed memory distinction, truthfulness/write boundaries, detailed implementation gaps, and explicit sign-off checklist.",
    "No production source code was modified in this turn."
  ],
  "decisions": [
    "The authoritative document is docs/mcp-probe-kit-delivery-system-v1.md and is now explicitly the final pre-development requirements baseline.",
    "Tool counts are locked by visibility: current Compact 23 / Compact+Memory 29 / Full 33 / Apps model-visible 29 / app-only 1 / unique names 34; target after architecture is 24 / 30 / 34 / 30 / 1 / 35.",
    "The document includes a final logical architecture diagram and a 35-row tool matrix covering current implementation path, current effect, target effect, and required changes.",
    "architecture owns ARC-8 and now has explicit input contract, unified output contract, implementation file layout, mode mapping, gates, and ten acceptance scenarios.",
    "fix_bug remains the single source of truth for SRC-8; start_bugfix only orchestrates or expands the shared method.",
    "Managed delivery flows require converge before persisting their MemoryCandidate, while explicit standalone memory administration may call memory tools directly with evidence and applicability.",
    "Plan/Heartbeat/Resume/Converge are required for managed long-running or formal delivery workflows, not for all direct read-only tools.",
    "Implementation is frozen into reversible phases: baseline; responsibility alignment; architecture; plan state/convergence; orchestrator closure; diff/verification consistency; memory quality.",
    "Exact stop/rollback conditions are part of the requirements, including unexpected tool counts, app-only leakage, host visibility changes, protocol regressions, project_root changes, undeclared writes, and overly coupled changes."
  ],
  "files_changed": [
    "docs/mcp-probe-kit-delivery-system-v1.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "git diff --check -- docs/mcp-probe-kit-delivery-system-v1.md passed.",
    "npm run docs:verify passed: 1067 checks, 34 documented model-visible tool entries."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "architecture, enhanced Plan/Converge, orchestrator closure, and diff comparison remain design-only and are not implemented.",
    "The final requirements document is Markdown in the repository; no separate DOCX was requested or generated."
  ],
  "next_actions": [
    "User reviews and signs off the section 15.9 development-start checklist.",
    "After acceptance, begin Phase 0 baseline capture and Phase 1 responsibility alignment; do not implement architecture before the baseline is preserved."
  ],
  "notes": "This checkpoint supersedes the earlier fragmented acceptance summaries. The single source for implementation requirements is docs/mcp-probe-kit-delivery-system-v1.md."
}
```

### auto-0a4f7a1031769563

```json
{
  "turn_id": "auto-0a4f7a1031769563",
  "timestamp": "unix:1785815964",
  "user_intent": "Begin engineering-grade implementation from the accepted requirements, starting with Phase 0 compatibility baseline and no production behavior changes.",
  "findings": [
    "Current runtime surfaces exactly match the accepted pre-architecture baseline: Compact 23, Compact+Memory 29, Full 33, Apps model-visible 29, App-only 1, unique callable 34.",
    "The existing contract audit previously checked representative calls and counts but did not fail on an exact missing/unexpected tool-name mismatch.",
    "Legacy and modern Compact surfaces currently match exactly.",
    "No production handler, schema, registry, visibility or runtime behavior was modified in Phase 0."
  ],
  "decisions": [
    "Phase 0 is implemented as a machine-readable compatibility contract rather than a prose-only snapshot.",
    "The baseline stores capability groups once and derives Compact, Compact+Memory, Full, Apps model-visible, App-only and unique-callable surfaces to avoid duplicated name lists.",
    "The existing audit:tools command now runs focused baseline validator tests and exact runtime set comparisons.",
    "Surface comparison is set-based rather than order-based, because tool order is not a public compatibility contract.",
    "App-only actions are checked both for hidden model visibility and direct negotiated callability.",
    "Implementation status is tracked separately from the frozen requirements document."
  ],
  "files_changed": [
    "config/tool-surface-baseline.json",
    "scripts/lib/tool-surface-baseline.mjs",
    "scripts/lib/__tests__/tool-surface-baseline.test.mjs",
    "scripts/tool-contract-audit.mjs",
    "package.json",
    "docs/verification/tool-surface-baseline-v4.0.0-rc.8.md",
    "docs/implementation/delivery-system-v1-progress.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "git diff --check passed.",
    "Tool-surface baseline validator tests passed 4/4.",
    "npm run audit:tools passed 38/38 runtime contract calls and exact surface checks.",
    "npm run smoke:protocol passed for legacy 23, modern 23 and full 33 surfaces.",
    "npm run docs:verify passed 1067 checks."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "Phase 1 responsibility wording is still inconsistent in workflow skill guidance and common flow examples.",
    "The production architecture tool and ARC-8 core do not exist yet.",
    "Phase 0 baseline must be intentionally updated in the same isolated commit that exposes architecture during Phase 2."
  ],
  "next_actions": [
    "Commit Phase 0 independently.",
    "Start Phase 1 by aligning Skill, Catalog, workflow guidance and tests around optional routing, start_* orchestration and direct atomic capability calls without changing public schemas.",
    "Do not expose architecture until its shared ARC-8 core, schemas and tests are ready in Phase 2."
  ],
  "notes": "The baseline validator will now stop accidental tool additions, removals, visibility leaks and App-only exposure before later architecture work proceeds."
}
```

### auto-e85e49c8e75746e2

```json
{
  "turn_id": "auto-e85e49c8e75746e2",
  "timestamp": "unix:1785816744",
  "user_intent": "Complete Phase 1 responsibility alignment with engineering-grade maintainability while preserving all public tool surfaces and behavior contracts.",
  "findings": [
    "The deterministic Agent eval initially detected that revised Catalog wording had dropped critical feature argument, first-heartbeat and convergence evidence terms; those terms were restored without reverting the new responsibility model.",
    "The generated Skill remains synchronized with all 33 current model-visible tools.",
    "Exact runtime surfaces remain Compact 23, Compact+Memory 29, Full 33, Apps model-visible 29, App-only 1 and unique callable 34.",
    "All full-suite and protocol tests pass after the final wording correction."
  ],
  "decisions": [
    "Skill and Catalog now distinguish direct domain/atomic capability calls from complete-delivery start_* orchestration.",
    "workflow remains an optional first-tool fallback and does not execute tools or own task lifecycle.",
    "fix_bug is described as the direct SRC-8 capability; start_bugfix is the complete-delivery orchestrator that composes or expands the same method.",
    "Plan, Heartbeat and Converge guidance applies to managed, resumable or formal delivery tasks rather than every single read-only call.",
    "Managed workflow MemoryCandidates require convergence before persistence, while explicit standalone Memory management remains directly callable.",
    "Critical existing requirements were preserved: complete start_feature description, spec_layout=auto, parent-child support, first heartbeat full plan, and convergence evidence terms.",
    "Phase 1 changes responsibility guidance, generated Skill semantics and regression tests only; no public schemas, handlers, tool names or visibility changed."
  ],
  "files_changed": [
    "src/server/tool-catalog.ts",
    "src/lib/mcp-tool-skill-registry.ts",
    "src/lib/workflow-skill-template.ts",
    "src/tools/workflow.ts",
    "src/lib/__tests__/mcp-tool-skill-registry.unit.test.ts",
    "docs/implementation/delivery-system-v1-progress.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "Focused Skill, workflow and registry tests passed 42/42.",
    "Deterministic Agent governance evals passed 2/2.",
    "Full regression suite passed 464/464 across 98 files.",
    "Tool-surface validator tests passed 4/4 and runtime contract audit passed 38/38.",
    "Dual-era protocol smoke passed: legacy 23, modern 23, full 33.",
    "Workflow Skill verification passed with 33 tools synchronized.",
    "Documentation verification passed 1067 checks.",
    "git diff --check passed."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "The architecture tool and ARC-8 production core are not implemented yet.",
    "Orchestrator completion loops, Plan lifecycle extensions, real diff comparison and Memory quality work remain in later phases."
  ],
  "next_actions": [
    "Commit Phase 1 independently.",
    "Begin Phase 2 by implementing the shared ARC-8 architecture methodology core, input/output schemas and unit tests before exposing the public architecture tool.",
    "Update the frozen tool-surface baseline only in the isolated commit that actually exposes architecture after its contracts pass."
  ],
  "notes": "No push, tag, release or unrelated user files were touched. The unrelated untracked CLAUDE.md and history index/session files remain excluded."
}
```

### auto-91cbe6358ea52c87

```json
{
  "turn_id": "auto-91cbe6358ea52c87",
  "timestamp": "unix:1785817051",
  "user_intent": "Begin Phase 2 with an engineering-grade shared ARC-8 methodology core and schemas before exposing the public architecture tool.",
  "findings": [
    "The repository has no npm typecheck script, so direct npx tsc --noEmit is the valid compiler check.",
    "Initial TypeScript compilation caught that alternative inputs were too strict; the input type was corrected to allow concise candidates while preserving complete output normalization.",
    "Focused ARC-8 tests cover seven method and gate scenarios and all pass."
  ],
  "decisions": [
    "ARC-8 is implemented in one shared architecture-method core rather than duplicated across tool modes or orchestrators.",
    "assess, design, validate and drift share one normalized result model and one set of gates.",
    "The first Phase 2 commit does not register or expose architecture; Catalog, Registry, Visibility and frozen tool counts remain unchanged until the handler and integration tests are ready.",
    "Architecture input accepts concise candidates and normalizes them into complete structured output.",
    "Design is blocked without current facts, structural causes, protected invariants, alternatives, a decision and target boundaries.",
    "Data or public contract changes require migration and rollback; drift requires real diff, target design and graph/runtime evidence.",
    "Drift detection is bounded and labels potential rule violations rather than claiming a complete static architecture scan."
  ],
  "files_changed": [
    "src/lib/architecture-method.ts",
    "src/schemas/architecture-tools.ts",
    "src/schemas/output/architecture-tools.ts",
    "src/lib/__tests__/architecture-method.unit.test.ts",
    "docs/implementation/delivery-system-v1-progress.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "ARC-8 methodology unit tests passed 7/7.",
    "npx tsc --noEmit passed after correcting the alternative input type."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "The public architecture tool is not yet registered or callable.",
    "Automatic project evidence collection and Memory recall are not yet connected to the tool adapter.",
    "Tool-surface target counts remain unchanged until the exposure commit."
  ],
  "next_actions": [
    "Commit the unexposed ARC-8 foundation independently.",
    "Implement architecture.ts as a thin adapter over architecture-method, with truthful guidance and optional delegated document plan.",
    "Add Schema/Catalog/Registry/Output/Visibility integration and update the frozen surface baseline only in the isolated exposure commit.",
    "Run unit, tool registry, CLI, protocol, audit and full regression tests before marking Phase 2 complete."
  ],
  "notes": "No existing tool behavior or model-visible surface changed in this substage."
}
```

### auto-5bb5742e7b6d768f

```json
{
  "turn_id": "auto-5bb5742e7b6d768f",
  "timestamp": "unix:1785819162",
  "user_intent": "Continue development and complete Phase 2 by productionizing the architecture tool and ARC-8 with maintainable engineering structure, full regression, protocol verification, and an independent commit.",
  "findings": [
    "The initial architecture-method implementation reached 581 lines, violating the repository maintainability threshold; it was split before commit.",
    "After the split, architecture production modules are 178, 219, 150, 142, and 360 lines.",
    "The architecture handler optionally collects code_insight and Memory evidence, preserves fact/inference/unknown distinctions, applies ARC-8 gates, and returns ArchitectureCandidate, ADR Candidate, and MemoryCandidate without claiming implementation completion.",
    "Release readiness, docs generation, Skill synchronization, Legacy/Modern protocol, CLI registry reuse, and all tool-surface counts recognize architecture consistently."
  ],
  "decisions": [
    "Expose exactly one new model-visible architecture tool with assess/design/validate/drift modes and ARC-8 as the single shared methodology source.",
    "Keep architecture directly callable; workflow only offers optional routing for explicit architecture work, while ordinary code understanding remains code_insight.",
    "Split ARC-8 into architecture-types, architecture-normalization, architecture-validation, and architecture-method so every new production module stays below 500 lines.",
    "Keep save_to_docs delegated; the MCP tool returns a document plan and does not directly rewrite project architecture documents.",
    "Update official tool surfaces to Compact 24, Compact+Memory 30, Full 34, Apps model-visible 30, App-only 1, unique callable 35.",
    "Preserve list_memory_assets as App-only and add no architecture MCP App UI."
  ],
  "files_changed": [
    "src/lib/architecture-method.ts",
    "src/lib/architecture-types.ts",
    "src/lib/architecture-normalization.ts",
    "src/lib/architecture-validation.ts",
    "src/tools/architecture.ts",
    "src/tools/__tests__/architecture.unit.test.ts",
    "src/schemas/architecture-tools.ts",
    "src/schemas/output/architecture-tools.ts",
    "src/schemas/index.ts",
    "src/tools/index.ts",
    "src/server/tool-catalog.ts",
    "src/server/tool-registry.ts",
    "src/server/tool-visibility.ts",
    "src/lib/output-schema-registry.ts",
    "src/lib/mcp-tool-skill-registry.ts",
    "src/lib/dev-workflow-routing.ts",
    "src/lib/dev-workflow.ts",
    "src/tools/workflow.ts",
    "config/tool-surface-baseline.json",
    "tools-manifest.json",
    "docs/data/tools.js",
    "docs/i18n/all-tools/zh-CN.json",
    "docs/i18n/all-tools/en.json",
    "docs/i18n/all-tools/ja.json",
    "docs/i18n/all-tools/ko.json",
    "docs/implementation/delivery-system-v1-progress.md"
  ],
  "tests": [
    "npx tsc --noEmit passed.",
    "Full npm test passed: 100 test files, 477 tests.",
    "npm run build passed.",
    "npm run audit:tools passed: 39/39 calls; Compact 24; Compact+Memory 30; Full 34; App-only 1; unique callable 35.",
    "npm run smoke:protocol passed for Legacy and Modern; Full surface 34.",
    "npm run release:static passed: 37 readiness checks, 0 errors, 0 warnings; docs verification 1080 checks passed.",
    "ARC-8 method/tool focused tests passed: 11/11."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "Phase 3 Plan lifecycle enhancements are not implemented yet.",
    "start_* complete-delivery closure and real diff/architecture drift integration remain Phase 4 and Phase 5 work.",
    "No push, tag, release, or package publication has been performed."
  ],
  "next_actions": [
    "Commit Phase 2 independently without push.",
    "Begin Phase 3 with backward-compatible Plan state fields, plan-driven evidence requirements, Heartbeat/Resume preservation, and Converge evaluation changes."
  ],
  "notes": "Unrelated untracked files CLAUDE.md and docs/history-session/1.md through 6.md, index.json, and .history.lock must remain untouched."
}
```

### auto-b59f97eec823ff86

```json
{
  "turn_id": "auto-b59f97eec823ff86",
  "timestamp": "unix:1785822268",
  "user_intent": "Continue MCP Probe Kit delivery-system implementation, complete Phase 3 Plan lifecycle changes, and investigate/fix the MCP App workbench bug where completed Feature/Bug work still displayed every Plan step as pending after refresh.",
  "findings": [
    "The screenshots exposed a real state-tracking defect: the workbench treated a successful resume_plan RPC as a successful state refresh even when no Plan checkpoint existed.",
    "No .mcp-probe-kit/plans checkpoint files existed for the reported Feature and Bug Plan IDs, so resume_plan had no stored completion state to return.",
    "resume_plan is intentionally read-only and cannot truthfully infer completed steps from chat text, Git diff or files; plan_heartbeat is the authoritative state writer.",
    "start_feature steady Plans previously stopped at specification, check and estimation despite claiming full delivery completion, so the workbench did not even have implementation, test and review steps to track.",
    "The workbench used the raw input project_root instead of the resolved bootstrap project root, which could read or create checkpoints in the wrong workspace.",
    "Phase 3 now makes convergence Plan-driven and preserves structured artifacts, candidates, acceptance results and runtime evidence compatibly."
  ],
  "decisions": [
    "Keep resume_plan read-only; do not infer or fabricate step completion from code changes or conversation text.",
    "When a workbench finds no checkpoint, create a marked initial checkpoint but keep it in an untracked/reconciliation state until an Agent verifies and writes real progress.",
    "Require managed Feature and Bug workflows to establish the first heartbeat with the full Plan and write a cumulative heartbeat after every completed, skipped or blocked step.",
    "Complete start_feature steady Plans with explicit implement, test and code_review steps; keep direct atomic tools unaffected by these managed-workflow rules.",
    "Resolve and persist projectRoot in declaredScope so all Plan state calls use one stable workspace.",
    "Commit Phase 3 and the workbench/orchestrator fix separately for reversibility."
  ],
  "files_changed": [
    "src/lib/delegated-plan-contract.ts",
    "src/plans/plan-state-metadata.ts",
    "src/plans/plan-types.ts",
    "src/plans/plan-heartbeat.ts",
    "src/plans/plan-store.ts",
    "src/plans/plan-converge.ts",
    "src/tools/plan_heartbeat.ts",
    "src/tools/resume_plan.ts",
    "src/tools/converge.ts",
    "src/schemas/plan-tools.ts",
    "src/schemas/output/plan-tools.ts",
    "src/server/tool-catalog.ts",
    "apps/mcp-probe-app.ts",
    "src/lib/plan-workbench-state.ts",
    "src/lib/delegated-plan-renderer.ts",
    "src/lib/mcp-apps.ts",
    "src/resources/generated/mcp-app-bundle.ts",
    "src/tools/start_feature.ts",
    "src/tools/start_bugfix.ts",
    "src/lib/__tests__/mcp-app-plan-state.unit.test.ts",
    "src/lib/__tests__/mcp-apps.unit.test.ts",
    "src/tools/__tests__/start_feature.unit.test.ts",
    "src/tools/__tests__/start_bugfix.unit.test.ts",
    "docs/implementation/delivery-system-v1-progress.md"
  ],
  "tests": [
    "Focused Plan metadata, workbench, Feature and Bugfix tests passed.",
    "Full regression passed: 102 test files, 492 tests, 0 failures.",
    "TypeScript noEmit check passed.",
    "Production build passed and synchronized 34 tools.",
    "Tool contract audit passed: 39/39.",
    "Legacy and Modern protocol smoke passed; Modern Plan state stored and converged.",
    "Release static verification passed: 37/37 checks, 0 errors, 0 warnings.",
    "Docs verification passed: 1080 checks across zh-CN, en, ja and ko.",
    "git diff --check passed; only expected LF-to-CRLF warnings were emitted."
  ],
  "runtime_state": [
    "Branch develop/v4.",
    "Phase 3 commit: f137704 feat: make plan convergence evidence-driven.",
    "First Phase 4/workbench fix commit: 39a4866 fix: synchronize workbench plan progress.",
    "No push, tag, release or package publication performed.",
    "Working tree should contain only the progress/history documentation update plus unrelated untracked CLAUDE.md and history-session support files before the final documentation commit."
  ],
  "remaining_issues": [
    "Existing historical executions that never called plan_heartbeat cannot be automatically reconstructed with certainty; the fixed workbench marks them untracked and requires Agent reconciliation.",
    "Phase 4 remains incomplete for start_ui, start_onboard, start_product and start_ralph.",
    "Phase 5 real diff, contract and architecture drift consistency remains pending.",
    "Phase 6 Memory conflict, supersede, expiry and retrieval-quality improvements remain pending."
  ],
  "next_actions": [
    "Commit the implementation progress and updated history checkpoint without including unrelated untracked files.",
    "Continue Phase 4 with one bounded orchestrator at a time, starting by auditing start_ui Plan contract and convergence closure.",
    "For each later workflow, preserve direct atomic tool usage and apply Plan/Heartbeat/Converge only to managed complete-delivery flows."
  ],
  "notes": "Do not add or modify unrelated untracked CLAUDE.md, docs/history-session/.history.lock, docs/history-session/1.md through 6.md, or docs/history-session/index.json."
}
```

### auto-1739782549b69a0c

```json
{
  "turn_id": "auto-1739782549b69a0c",
  "timestamp": "unix:1785824333",
  "user_intent": "Continue Phase 4 with an engineering-grade, independently reversible closure of the start_ui delivery orchestrator before moving to start_onboard.",
  "findings": [
    "The old start_ui implementation had three duplicated ad hoc plans and separate auto/manual report summaries, so Plan steps, displayed report and execution guidance could drift.",
    "The old UI flow ended after visual acceptance and lacked explicit state/accessibility validation, actual test evidence, code review, architecture drift and formal convergence guidance.",
    "Array order was used as implicit execution order in several UI steps; resume_plan requires explicit dependsOn relationships.",
    "After refactoring, start_ui.ts is 473 lines; start-ui-plan.ts 370; start-ui-request.ts 230; start-ui-config.ts 129; start-ui-output.ts 127."
  ],
  "decisions": [
    "All start_ui modes use one shared DelegatedPlanContract and one generated report; no mode maintains a separate plan truth source.",
    "The UI delivery plan includes real implementation, desktop/mobile screenshot evidence, visual iteration, state/accessibility acceptance, tests, code review, conditional architecture drift, context update and optional MemoryCandidate preparation.",
    "Every UI plan step has explicit dependencies so resume_plan restores a real next step rather than relying on array order or prior chat.",
    "Architecture drift is conditional: required when ArchitectureCandidate/ADR or boundaries/contracts are involved, otherwise the step must be explicitly skipped with a reason.",
    "start_ui remains a planning/orchestration tool and does not claim Agent file, runtime, screenshot, test or review actions were performed.",
    "The large start_ui module was split into request, configuration, plan and output modules; all production modules remain below 500 lines."
  ],
  "files_changed": [
    "src/lib/delegated-plan-renderer.ts",
    "src/tools/start_ui.ts",
    "src/tools/start-ui-plan.ts",
    "src/tools/start-ui-request.ts",
    "src/tools/start-ui-config.ts",
    "src/tools/start-ui-output.ts",
    "src/tools/__tests__/start_ui.unit.test.ts",
    "src/tools/__tests__/start_ui.property.test.ts",
    "docs/implementation/delivery-system-v1-progress.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "npx tsc --noEmit passed.",
    "start_ui focused tests passed: 48/48 across 4 files.",
    "Full regression passed: 496/496 across 102 test files.",
    "npm run build passed.",
    "npm run audit:tools passed: 39/39; surfaces remain 24/30/34/30/1/35.",
    "npm run smoke:protocol passed for Legacy, Modern and full compatibility surfaces.",
    "npm run release:static passed: 37/37 release checks and 1080 docs checks."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "start_onboard still primarily orchestrates init_project_context and lacks complete project mental-model, command, graph, Memory and formal convergence closure.",
    "start_product and start_ralph remain incomplete Phase 4 workflows.",
    "Phase 5 diff/verification consistency and Phase 6 Memory quality remain pending."
  ],
  "next_actions": [
    "Commit the start_ui Phase 4 slice independently without push.",
    "Continue Phase 4 with a bounded audit and closure of start_onboard.",
    "After onboard, close start_product and start_ralph independently."
  ],
  "notes": "No push, tag, release or npm publication. Unrelated untracked CLAUDE.md and docs/history-session/1.md through 6.md, index.json and .history.lock remain untouched."
}
```

### auto-43462adfd097adfa

```json
{
  "turn_id": "auto-43462adfd097adfa",
  "timestamp": "unix:1785825135",
  "user_intent": "Continue Phase 4 by closing start_onboard as a complete, maintainable and independently reversible project onboarding workflow.",
  "findings": [
    "The previous start_onboard only wrapped init_project_context in a one-step ad hoc plan and could not produce a genuine project mental model or verified quickstart.",
    "Onboarding does not require code_review or architecture by default because it is a read-only understanding workflow; code_insight plus evidence-backed Agent analysis is the appropriate capability set.",
    "The new modules remain small: start_onboard.ts 159 lines and start-onboard-plan.ts 257 lines."
  ],
  "decisions": [
    "start_onboard is a read-only managed workflow with requiredEvidenceKinds=[] and explicit quality gates rather than implementation/test/review evidence requirements.",
    "The onboarding plan covers project instructions, context, code graph, manifests/runtime configuration, entry points/module/data flows, real command extraction and safe verification, constraints/risks, quickstart/navigation documentation, acceptance and optional project-knowledge MemoryCandidate preparation.",
    "Commands not actually executed must remain candidates; verification requires command, cwd and exit code, and onboarding never installs dependencies or starts long-running services automatically.",
    "All onboarding conclusions distinguish fact, inference and unknown and must cite current repository/config/graph/command evidence.",
    "docs_dir is restricted to a safe project-relative path, and project_root is resolved and pinned in declaredScope.",
    "All steps have explicit dependencies and use the shared Heartbeat/Resume/Converge protocol."
  ],
  "files_changed": [
    "src/tools/start_onboard.ts",
    "src/tools/start-onboard-plan.ts",
    "src/tools/__tests__/start_onboard.unit.test.ts",
    "docs/implementation/delivery-system-v1-progress.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "npx tsc --noEmit passed.",
    "start_onboard focused tests passed: 3/3.",
    "Full regression passed: 498/498 across 102 test files.",
    "npm run build passed.",
    "npm run audit:tools passed: 39/39; surfaces remain 24/30/34/30/1/35.",
    "npm run smoke:protocol passed for Legacy, Modern and full compatibility surfaces.",
    "npm run release:static passed: 37/37 release checks and 1080 docs checks."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "start_product still lacks complete product artifact acceptance, state recovery and formal convergence closure.",
    "start_ralph still lacks unified Plan, per-round heartbeat evidence, bounded stop/failure policy and final convergence.",
    "Phase 5 diff/verification consistency and Phase 6 Memory quality remain pending."
  ],
  "next_actions": [
    "Commit the start_onboard Phase 4 slice independently without push.",
    "Continue Phase 4 with start_product, preserving product-delivery evidence semantics rather than requiring code implementation.",
    "Close start_ralph independently, then run the final Phase 4 regression."
  ],
  "notes": "No push, tag, release or npm publication. Unrelated untracked CLAUDE.md and docs/history-session/1.md through 6.md, index.json and .history.lock remain untouched."
}
```

### auto-e80c074d7d44c8b8

```json
{
  "turn_id": "auto-e80c074d7d44c8b8",
  "timestamp": "unix:1785825803",
  "user_intent": "Continue Phase 4 by closing start_product as a formal product artifact and prototype delivery workflow without falsely requiring software implementation evidence.",
  "findings": [
    "The previous product flow had no explicit Heartbeat/Resume/Converge protocol and no product acceptance quality gates.",
    "The previous flow described ui_design_system as writing files even though the tool only returns structured content and guidance.",
    "The previous parent flow could treat a start_ui tool response as an implemented prototype even though start_ui returns a child Plan.",
    "The new modules are bounded: start_product.ts 268 lines and start-product-plan.ts 351 lines."
  ],
  "decisions": [
    "start_product uses product-specific required evidence (requirements and review) and quality gates; it does not require production implementation, software tests or code_review.",
    "Material product gaps become an explicit user-input step and must be closed, accepted as assumptions or recorded as blockers.",
    "ui_design_system is represented truthfully as returning a structured contract; a separate Agent action persists design-system JSON and Markdown.",
    "start_ui is a child delegated workflow; the parent html-prototype step is complete only after recording child plan_id, real prototype artifacts, screenshot acceptance and child converge passed=true.",
    "The product package receives dedicated PRD review, prototype consistency/visual acceptance and final product-package review before parent convergence.",
    "Memory recall and MemoryCandidate preparation are optional and remain gated by the parent plan.",
    "docs_dir and requirements_file are contained within the resolved project root."
  ],
  "files_changed": [
    "src/tools/start_product.ts",
    "src/tools/start-product-plan.ts",
    "src/tools/__tests__/start_product.unit.test.ts",
    "docs/implementation/delivery-system-v1-progress.md",
    "docs/history-session/7.md"
  ],
  "tests": [
    "npx tsc --noEmit passed.",
    "start_product focused tests passed: 5/5.",
    "Full regression passed: 501/501 across 102 test files.",
    "npm run build passed.",
    "npm run audit:tools passed: 39/39; surfaces remain 24/30/34/30/1/35.",
    "npm run smoke:protocol passed for Legacy, Modern and full compatibility surfaces.",
    "npm run release:static passed: 37/37 release checks and 1080 docs checks."
  ],
  "runtime_state": [],
  "remaining_issues": [
    "start_ralph remains the only incomplete Phase 4 orchestrator.",
    "Phase 5 diff/verification consistency and Phase 6 Memory quality remain pending."
  ],
  "next_actions": [
    "Commit the start_product Phase 4 slice independently without push.",
    "Close start_ralph with a formal bounded-round plan, per-round heartbeat evidence, stop/failure policy and final convergence.",
    "Run the final Phase 4 regression and mark Phase 4 complete."
  ],
  "notes": "No push, tag, release or npm publication. Unrelated untracked CLAUDE.md and docs/history-session/1.md through 6.md, index.json and .history.lock remain untouched."
}
```

